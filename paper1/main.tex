\documentclass[11pt]{article}

%% --- Geometry / typography ---------------------------------------------------
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

%% --- Math / typography helpers ----------------------------------------------
\usepackage{amsmath,amssymb,amsthm}
\usepackage{bm}

%% --- Tables, figures, captions ----------------------------------------------
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{array}
\usepackage{graphicx}
\graphicspath{{../assets/paper1_figs/}{assets/paper1_figs/}{figures/}}
\usepackage{float}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}

%% --- Code listing -----------------------------------------------------------
\usepackage{xcolor}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  columns=fullflexible,
  keywordstyle=\color{blue!60!black}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{purple!50!black},
  language=Python,
  showstringspaces=false,
  numberstyle=\tiny\color{gray},
  frame=single, framerule=0.3pt, framesep=4pt,
  backgroundcolor=\color{gray!4},
  xleftmargin=4pt,
  literate={σ}{{$\sigma$}}1 {ρ}{{$\rho$}}1 {ε}{{$\varepsilon$}}1
}

%% --- Hyperlinks / refs ------------------------------------------------------
\usepackage[hidelinks]{hyperref}
\usepackage{url}
\usepackage{cleveref}

%% --- Misc -------------------------------------------------------------------
\usepackage{enumitem}
\setlist{nosep,leftmargin=*}
\usepackage[normalem]{ulem}
% seqsplit lets long monospace identifiers (e.g. snake_case metric names) break
% across line ends, eliminating overfull \hbox warnings on long \code{...} runs.
\usepackage{seqsplit}
% Allow extra inter-word space on the last pass to absorb the occasional
% paragraph that microtype alone cannot fit — keeps the right margin clean
% on lines that would otherwise overflow by a few points.
\emergencystretch=3em

%% --- Macros -----------------------------------------------------------------
% \code{...} renders a short identifier in monospace; for verbose code names we
% provide a hyphen-permissive variant \codebrk{...} that allows line breaks at
% underscores. \fragmetric prints the full code identifier (used in tables and
% on first introduction); \fragility prints the short prose name "fragility
% ratio" used in body text to avoid long-line overflows.
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\codebrk}[1]{\texttt{\small\seqsplit{#1}}}
\newcommand{\stdmax}{\code{std\_max}}
% Use seqsplit so the long identifier can break across line ends in body text.
\newcommand{\fragmetric}{\codebrk{predictor\_target\_to\_nn\_cos\_ratio\_at\_max\_std}}
\newcommand{\fragility}{fragility ratio}
\newcommand{\transres}{\code{transition\_resolution\_ratio}}
\newcommand{\idprobe}{\code{id\_probe\_r{}\textsuperscript{2}}}
\newcommand{\dataset}[1]{\code{assets/paper1\_data/#1}}

\title{\textbf{Latent Prediction Is Not Visual Robustness:\\Diagnosing the Invariance--Resolution Trade-off\\in JEPA World Models for Control}}
\author{}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Joint-Embedding Predictive Architectures (JEPAs) are commonly \emph{believed} to learn abstract, invariant world representations: by predicting in latent space rather than reconstructing pixels, the encoder is expected to discard visual redundancy and noise. This expectation is a \emph{community heuristic} rather than a published guarantee --- to our knowledge no JEPA work formally claims pixel-noise robustness for control. We test the implicit assumption on \textbf{LeWorldModel (LeWM)}~\cite{maes2026lewm}, a published JEPA world model, across four manipulation and navigation tasks (PushT, TwoRoom, Reacher, Cube) and eight levels of train-time pixel-noise augmentation. We find three things:
\begin{enumerate}
  \item \textbf{Visual out-of-distribution (OOD) collapse in JEPA + Cross-Entropy Method (CEM) control.} Without noise-aware training, LeWM collapses under mild pixel noise: PushT control success drops from $86.33\%$ (clean) to $4.67\%$ (Gaussian \mbox{std = 0.08}, near-random); TwoRoom drops from $94.00\%$ to $50.00\%$.
  \item \textbf{No globally optimal noise level exists.} Tasks respond very differently to noise augmentation: visually redundant TwoRoom prefers heavy noise (point-best at \stdmax{} $= 0.008$), while contact-heavy PushT peaks on \emph{clean} at \stdmax{} $= 0.003$ but on \emph{robustness} at \stdmax{} $= 0.006$ --- clean and robust optima dissociate within a single task.
  \item \textbf{A five-layer diagnostic protocol explains the compression mechanism.} Instrumenting encoder shift, encoder geometry, predictor sensitivity, latent-noise response, and task resolution traces noise-induced control failure to a representational chain: representation compression (drop in effective rank) $\rightarrow$ loss of transition-key resolution $\rightarrow$ loss of controllability (drop in the inverse-dynamics probe $R^2$). However, when used as a \emph{cross-checkpoint} predictor, the strongest single diagnostic --- which we call the \emph{fragility ratio} (Section~\ref{sec:bg-five-layers}) --- tracks \emph{overall checkpoint quality on clean inputs}, not OOD-specific robustness. The toolkit usefully ranks checkpoints by clean control fidelity but does not substitute for actually training with noise when OOD robustness is the goal.
\end{enumerate}
This paper does not propose a new training algorithm. Its contribution is (i) a systematic empirical study of JEPA + CEM visual OOD failure, (ii) a reproducible diagnostic toolkit, and (iii) an explicit delineation of what cross-checkpoint diagnostics can and cannot predict.

\smallskip
\noindent\textbf{Keywords:} world models; JEPA; visual robustness; representation diagnostics; invariance--resolution trade-off.
\end{abstract}

%% ===========================================================================
\section{Introduction}\label{sec:intro}

\subsection{The implicit invariance heuristic and where it breaks}\label{sec:intro-heuristic}

Since Yann LeCun proposed JEPA~\cite{lecun2022path}, the paradigm has been advanced as a direction for self-supervised learning. Unlike generative models (variational autoencoders, diffusion), JEPA does not reconstruct pixels; it predicts future \emph{representations} in latent space. The motivating intuition --- that predicting ``what is invariant'' rather than ``what pixels look like'' yields abstract representations that discard visual redundancy and noise --- is now part of the field's informal vocabulary in talks, blog posts, and survey articles~\cite{assran2023ijepa,bardes2024vjepa}.

This is a heuristic rather than a published guarantee. To our knowledge no JEPA paper has \emph{formally claimed} visual-OOD robustness for control-relevant downstream tasks. I-JEPA~\cite{assran2023ijepa} and V-JEPA~\cite{bardes2024vjepa,assran2025vjepa2} established strong visual representations on ImageNet and video via masked prediction; LeWorldModel (LeWM)~\cite{maes2026lewm} extended the framework to end-to-end stable world-model training across four robotic control tasks. Existing robustness studies probe JEPA only on image classification~\cite{njepa2025}, synthetic 1D distractors~\cite{huang2026vjepa}, or medical ultrasound~\cite{usjepa2025}; none on \emph{JEPA-based control} under realistic pixel noise.

This leaves a basic operational question open: \textbf{if the input image is degraded by sensor noise, lighting change, or camera jitter, does a JEPA + CEM world model still plan and act reliably?}

The empirical answer is no. On PushT (2D pushing), the untrained-with-noise LeWM achieves $86.33\%$ on clean images but falls to $4.67\%$ under Gaussian pixel noise of \mbox{std = 0.08} --- near-random. TwoRoom (2D navigation) drops from $94.00\%$ to $50.00\%$. Latent prediction alone does not, in this regime, confer the visual robustness the community heuristic would predict.

\subsection{The core tension: no globally optimal noise level}\label{sec:intro-tension}

A natural remedy is input-side noise augmentation during training, a technique long-validated in supervised and contrastive learning~\cite{chen2020simclr,cubuk2020randaugment}. But a deeper question arises: \textbf{does there exist a single, universal noise level that is optimal across tasks?}

A systematic sweep across four tasks at eight levels of \stdmax{} $\in \{0.001, \dots, 0.008\}$ answers no:
\begin{itemize}
  \item \textbf{TwoRoom} (visually redundant navigation): clean success rises monotonically with noise, peaking at \mbox{\stdmax{} = 0.008} ($98.33\%$ / $98.67\%$).
  \item \textbf{PushT} (contact-heavy manipulation): clean peaks at \mbox{\stdmax{} = 0.003} ($89.67\%$), but robustness at px+goal 0.08 peaks at \mbox{\stdmax{} = 0.006} ($87.00\%$) --- clean and robust optima dissociate.
  \item \textbf{Reacher} (continuous reaching): lies on a \mbox{$0.002$--$0.006$} plateau; clean point-best is at \mbox{\stdmax{} = 0.006} ($86.00\%$), while px+goal 0.08 point-best is at \mbox{\stdmax{} = 0.002} ($85.67\%$). Very low noise ($0.001$) is statistically indistinguishable from base.
  \item \textbf{Cube} (structured 3D manipulation): noise sweep is the weakest of the four, with no monotone trend on clean. We read this as the trade-off's mildest manifestation rather than an absence: a structured action sequence and predictable visual-action coupling supply enough robustness that input-side augmentation buys comparatively little.
\end{itemize}

This exposes a fundamental tension: \textbf{global noise augmentation cannot distinguish ``background visual redundancy that should be made invariant'' from ``control-relevant features that should retain resolution''.}

\subsection{Contributions}\label{sec:intro-contributions}

\paragraph{C1.} A systematic quantification of visual-OOD fragility of JEPA + CEM control across four representative tasks. We sweep 4 tasks $\times$ 8 noise levels, and evaluate all 36 checkpoints under a unified 3-evaluation-seed $\times$ 100-trajectory protocol, for 300 trajectories per condition.

\paragraph{C2.} The ``invariance--resolution trade-off'' concept and a five-layer diagnostic protocol that operationalises it. The protocol covers encoder shift, encoder geometry, predictor sensitivity, latent-noise response, and task resolution, and bundles 17 concrete metrics (Section~\ref{sec:bg-five-layers}). We validate it by Spearman correlations on the 4-task $\times$ $n = 9$ LeWM sweep under unified 3-seed $\times$ 100 evaluation, with partial correlations conditioned on training noise to separate residual checkpoint-quality signal from the monotone sweep trend induced by \stdmax{}.

\paragraph{C3.} A mechanistic account of why global noise augmentation has limited returns. On TwoRoom (visually redundant) the gains coincide with desirable representation compression --- effective rank drops by roughly $30\%$ ($47.60 \to 33.59$) and the more compact latent is easier to plan in. On PushT (contact-heavy), the base representation already has higher rank and higher controllability (effective rank $76.42$; inverse-dynamics probe $R^2 \approx 0.77$); even the light representative noise-trained checkpoint compresses rank by about $44\%$ ($76.42 \to 42.85$) with concomitant downward movement in transition-resolution metrics and the controllability probe (both drop by a few points; see Table~\ref{tab:diag-base-vs-best}), so further compression cannot be assumed safe for contact-heavy tasks.

\paragraph{C4.} An explicit delineation of cross-checkpoint diagnostics. The strongest cross-checkpoint diagnostic (the \fragility{}) carries a residual checkpoint-quality signal beyond the sweep-level \stdmax{} effect (partial Spearman $\rho = -0.59$ on clean and $-0.41$ on px+goal 0.08 after conditioning on \stdmax{}, on the $n = 9$ LeWM PushT sweep with unified 3-seed $\times$ 100 eval). However, the apparent $\rho(\text{metric}, \text{OOD drop}) = -0.77$ is mediated by \stdmax{}: after partialling out \stdmax{}, that correlation collapses to $+0.06$. The metric is a model-selection tool after controlling for the sweep-level \stdmax{} trend, not an OOD oracle (\Cref{sec:diag-cross,sec:disc-boundary}).

\subsection{Organisation}
\Cref{sec:related} reviews related work; \Cref{sec:bg} defines the LeWM background, noise protocol, and diagnostic framework; \Cref{sec:exp} reports the experimental findings; \Cref{sec:disc} discusses mechanism and implications; \Cref{sec:conc} concludes.

%% ===========================================================================
\section{Related Work}\label{sec:related}

\subsection{JEPA and latent world models}
JEPA~\cite{lecun2022path} predicts in latent space rather than reconstructing pixels. I-JEPA~\cite{assran2023ijepa} predicts target representations from a masked context; V-JEPA~\cite{bardes2024vjepa,assran2025vjepa2} extends the framework to video understanding and video-driven world modelling; LeWM~\cite{maes2026lewm} is the first end-to-end stable JEPA world model, using \textbf{SIGReg} (Sketch Isotropic Gaussian Regularizer) --- random projections plus Epps--Pulley empirical-characteristic-function matching~\cite{eppspulley1983} --- to prevent representation collapse without batch normalisation. LeWM is our baseline. The original LeWM paper reports a Violation-of-Expectation experiment showing the model is sensitive to physical perturbations (object teleportation) but not to visual perturbations (colour change). VoE however measures prediction error (surprise), not control success rate, and colour change differs from pixel-level Gaussian noise. We give the first picture, to our knowledge, of LeWM's control success rate under pixel-noise corruption.

For one external-baseline sanity check we also evaluate PLDM~\cite{sobal2022jointembeddingpredictivearchitectures,sobal2025stresstesting} as implemented in \code{stable-worldmodel}~\cite{maes2026stableworldmodel}. We use it only to test whether the clean-trained visual-noise cliff is isolated to LeWM; the full sweep, diagnostic correlations, and trade-off claims remain LeWM-only.

\subsection{Robustness studies of JEPA}
N-JEPA~\cite{njepa2025} introduces diffusion-noise augmentation into I-JEPA, improving linear-probing robustness on ImageNet. VJEPA~\cite{huang2026vjepa} tests a Noisy-TV distractor on synthetic 1D signals and reports JEPA retains $R^2 > 0.84$ under high noise. US-JEPA~\cite{usjepa2025} tests Gaussian blur, contrast reduction, and speckle noise on medical ultrasound. The contemporaneous Bisim-JEPA~\cite{toso2026bisimjepa} attacks the related problem of slow-feature distractors in JEPA-based planning by augmenting the predictive objective with a bisimulation encoder that maps states with similar dynamics to nearby latents. Their intervention (bisimulation regularizer for control-relevant invariance) is complementary to ours: we instrument an unmodified LeWM under pixel noise and report what its latents \emph{do and do not} retain, while they propose an inductive bias that targets a different corruption family (slow-feature distractors). None of these studies the pixel-noise robustness of a JEPA \emph{world model} on \emph{robotic control} with a cross-checkpoint diagnostic--evaluation correlation analysis. Furthermore, VJEPA's optimistic conclusion ($R^2 > 0.84$ in 1D) contrasts with our control-time observation (success rate $\to 4.67\%$), suggesting that the ``natural robustness'' of JEPA may be an artefact of evaluation modality.

\subsection{World models and input augmentation}
Input-side image augmentation is an established route to visual robustness in pixel-based reinforcement learning: DrQ~\cite{kostrikov2020drq} and DrQ-v2~\cite{yarats2022drqv2} regularise a model-free critic with random-shift augmentation; SODA / DMC-GB~\cite{hansen2021soda} formalises a visual-generalisation benchmark on DeepMind Control with random colours and video backgrounds. Both lines establish that augmentation pressure during training is a strong baseline for closing the OOD gap --- but they study model-free policies with explicit reward signals, not JEPA-style latent-prediction world models with planning-time CEM control. In the model-based RL world-model literature, DreamerV3~\cite{hafner2025dreamerv3} and TD-MPC2~\cite{hansen2024tdmpc2} rely on learned visual encoders and latent dynamics, which may provide some implicit tolerance to benign visual variation but do not by themselves settle sensor-noise robustness. ViGMO~\cite{vigmo} tests Gaussian noise and blur on DMC tasks, finds ``sensor noise is a fundamentally different distribution shift'', and proposes a latent-consistency loss. ViGMO addresses model-based RL, not JEPA architectures; its conclusion is directionally aligned with ours, but our contribution adds mechanistic decomposition via the five-layer diagnostic, not just performance reporting.

\subsection{The invariance--resolution tension}
The idea that representation learning balances two competing properties is well established in the self-supervised literature. Wang and Isola~\cite{wang2020alignuniform} decompose the contrastive loss into an \emph{alignment} term (positive pairs close together) and a \emph{uniformity} term (features spread on the hypersphere), and show that downstream linear-probe accuracy tracks both in concert. Tamkin et al.~\cite{tamkin2023featuredropout} argue, in the contrastive-learning context, that label-destroying augmentations can be useful --- augmentations act as feature dropout rather than pure invariance inducers. Zhang and Ma~\cite{zhang2022rethinkaug} note that augmentation modules can impose invariances that must be matched to the task. Concurrent seq-JEPA~\cite{ghaemi2025seqjepa} resolves a related \emph{invariance--equivariance} tension in JEPA by introducing architectural disentanglement of two heads. Our contribution is conceptually adjacent but operationally distinct: we study a \emph{compression-versus-control-resolution} trade-off observed empirically in latent predictive world models, where the downstream task is planning rather than discrimination, and we operationalise it through a diagnostic protocol rather than a new architecture or loss.

\subsection{Representation diagnostics and collapse analysis}
Self-supervised learning broadly uses effective rank~\cite{roy2007effrank}, condition number, and participation ratio to diagnose dimensional collapse~\cite{jing2022dimcollapse}. Next-Latent Prediction~\cite{teoh2025nextlatent} uses effective latent rank to assess world-model compactness. VICReg~\cite{bardes2022vicreg} provides an anti-collapse regularizer distinct from SIGReg. Centered Kernel Alignment (CKA)~\cite{kornblith2019cka} compares representations across networks or training conditions; we use it to compare clean-input and noisy-input latents within a checkpoint. Linear probes for representation analysis follow Alain and Bengio~\cite{alain2017linearprobes}. Most relevantly, RankMe~\cite{garrido2023rankme} showed that a label-free effective-rank diagnostic correlates with downstream linear-probe transfer across hyperparameter sweeps in self-supervised learning. Our cross-checkpoint correlation analysis (\Cref{sec:diag-cross}) asks the analogous question for control: \emph{does a label-free predictor-sensitivity diagnostic correlate with downstream task success after the sweep-level training trend is removed?} Individual metrics are not new; our contribution is to combine them into a coherent five-layer protocol with per-token noise sensitivity, cross-checkpoint correlation, and a partial-correlation validation scheme conditioning on training noise --- and to delineate where that programme succeeds and where it does not.

%% ===========================================================================
\section{Background and Diagnostic Framework}\label{sec:bg}

\subsection{LeWorldModel baseline}\label{sec:bg-lewm}

LeWM~\cite{maes2026lewm} is an end-to-end JEPA world model trained with two terms only:
\begin{equation}
  \mathcal{L}_{\text{LeWM}} \;=\; \mathcal{L}_{\text{pred}} \;+\; \lambda \,\cdot\, \mathcal{L}_{\text{SIGReg}}\,,
  \label{eq:lewm-loss}
\end{equation}
where $\mathcal{L}_{\text{pred}}$ is the latent-space mean-squared error (MSE) between predicted and target representations. SIGReg projects each latent onto $M$ unit-norm random directions, computes the Epps--Pulley empirical-characteristic-function distance~\cite{eppspulley1983} between each projection and $\mathcal{N}(0, 1)$, and aggregates with Gauss-window weights, preventing collapse without explicit BatchNorm. (The Cramér--Wold theorem motivates the construction: equality of high-dimensional distributions reduces to equality of all one-dimensional projections of their characteristic functions.) Inference uses the Cross-Entropy Method (CEM)~\cite{williams2007cem} for latent-space model predictive control (MPC)~\cite{garcia1989mpc}.

\subsection{Input-side noise augmentation}\label{sec:bg-noise}

We add per-frame Gaussian noise to the LeWM input pipeline via \codebrk{utils.py::AddNormalizedGaussianNoise} (\Cref{sec:appendix-A}). Each frame is independently noised: $\mathrm{Bernoulli}(p)$ decides whether the frame is corrupted, and if so the standard deviation is drawn from $\mathrm{Uniform}(0, \text{std\_max})$. We fix $p = 1.0$ and sweep \stdmax{} $\in \{0.001, \dots, 0.008\}$ (eight levels). Evaluation uses two noised conditions: pixels+goal $0.05$ and pixels+goal $0.08$ (Gaussian noise of the indicated std applied to both pixels and the goal image).

\subsection{Five-layer diagnostic framework}\label{sec:bg-five-layers}

The framework decomposes the JEPA + CEM forward pass --- pixels $\to$ encoder $f$ $\to$ latent $z$ $\to$ predictor $g$ $\to$ cost surface $\to$ planned actions --- into five sequential stages and instruments each transition with one or more metrics. Layers~1--2 characterise the encoder output (how the latent shifts under noise; how the latent space is globally organised); Layer~3 measures how the predictor amplifies that shift; Layer~4 isolates the predictor and cost surface by injecting noise directly into the latent; Layer~5 quantifies the planning-relevant information that the latent still carries. Each layer probes one transition, so an observed failure can be localised to a single stage. Most individual metrics already have a literature home (cited inline below); the contribution is the composite protocol --- and a handful of metrics introduced for this paper (the \emph{\fragility{}}, \transres{}, \code{latent\_robust\_radius\_z}) that explicitly normalise predictor and cost-surface sensitivity by the local nearest-neighbour scale of the clean encoder. We use nearest-neighbour distance as a local latent scale primitive in the spirit of kNN-based OOD detection~\cite{sun2022knnood}, but the scale-normalised ratios below are introduced for this diagnostic protocol.

\paragraph{Minimal notation.} Let $z_i=f(x_i)$ and $\tilde z_i=f(\tilde x_i)$ be clean and noised latents for the same token, and let $d_{\cos}(a,b)=1-\frac{a^{\top}b}{\lVert a\rVert\lVert b\rVert}$. Define the clean local scale $d_i^{\mathrm{NN}}=\min_{j\ne i}d_{\cos}(z_i,z_j)$. The main introduced ratios are:
\begin{align}
  r_i^{\mathrm{enc}} &= \frac{d_{\cos}(z_i,\tilde z_i)}{d_i^{\mathrm{NN}}+\epsilon}\,,
  &
  r_i^{\mathrm{pred}} &= \frac{d_{\cos}\!\big(g(z)_i, g(\tilde z)_i\big)}{d_i^{\mathrm{NN}}+\epsilon}\,,
  \label{eq:diag-ratios}\\
  \transres{}_{d} &= \frac{\operatorname{median}_{t}\, d(z_t,z_{t+1})}
  {\operatorname{median}_{(t,t')\in\mathcal{F}}\, d(z_t,z_{t'})+\epsilon}\,,
  \label{eq:transition-resolution}
\end{align}
where $r_i^{\mathrm{enc}}$ corresponds to \code{noise\_to\_nn\_cos\_ratio}, $r_i^{\mathrm{pred}}$ to the \fragility{}, $d$ is either cosine or L2 distance for \transres{}, and $\mathcal{F}$ denotes random far pairs from different temporal locations. Layer~1 otherwise uses standard Euclidean/cosine displacements; no method-specific external citation is needed for the raw angle or L2 quantities.

\paragraph{Layer 1 --- Encoder shift.} The magnitude and direction of latent displacement under input noise. Metrics: \code{noise\_angle\_deg}, \code{noise\_l2}, \code{noise\_to\_nn\_cos\_ratio} (encoder shift normalised by the clean nearest-neighbour (NN) cosine distance --- introduced here), \code{noise\_angle\_slope} (the per-unit-std angular gain).

\paragraph{Layer 2 --- Encoder geometry.} The global structure of the encoded latent space. Metrics: \code{clean\_nn\_cos\_dist} (local neighbourhood scale), \code{clean\_effective\_rank}~\cite{roy2007effrank} (intrinsic dimensionality), and \code{cka\_linear\_at\_max\_std} (CKA~\cite{kornblith2019cka} between clean-input and noisy-input latents at the diagnostic's max injection std).

\paragraph{Layer 3 --- Predictor sensitivity.} How the predictor amplifies the encoder shift. Metrics: the \emph{\fragility{}} (single-step predictor target shift normalised by clean NN distance, stored as \fragmetric{} --- introduced here; the strongest cross-checkpoint diagnostic we identify), and \code{predictor\_rollout\_drift\_T(T)} (multi-step rollout L2 drift at horizon $T$).

\paragraph{Layer 4 --- Latent-noise response.} Noise injected directly into the latent $z$ (bypassing the encoder) isolates predictor and cost contributions. Metrics: \code{latent\_cost\_surface\_slope\_z} (slope of the planner's cost surface vs latent perturbation magnitude) and \code{latent\_robust\_radius\_z} (the largest perturbation that keeps the cost ranking stable --- introduced here).

\paragraph{Layer 5 --- Task resolution.} The control-relevant information retained in the latent. Metrics: \transres{}\_l2 and \transres{}\_cos (the ratio of inter-transition to intra-transition distance under L2 / cosine geometries --- introduced here as a planning-relevant analogue of class separability), and \idprobe{}, the inverse-dynamics linear-probe $R^{2}$ (a controllability proxy in the spirit of linear-probe analysis~\cite{alain2017linearprobes}).

\subsection{Cross-checkpoint validation protocol}\label{sec:bg-cross-ckpt}

To rule out spurious correlations we adopt two complementary analyses on the same LeWM PushT sweep:

\begin{description}
  \item[LeWM $n = 9$ sweep with partial correlation.] All 9 LeWM checkpoints per task (base + \stdmax{} $\in \{0.001, \dots, 0.008\}$). Compute Spearman $\rho$ and \emph{partial} Spearman conditioned on \stdmax{}. The partialling step matters: many diagnostics correlate with control performance only because both quantities co-vary with the training noise level along the sweep. The relevant question is whether a diagnostic retains a residual checkpoint-quality signal after removing the monotone sweep trend associated with \stdmax{}.
\end{description}

We report both the raw Spearman and the partial-on-\stdmax{} quantities. The raw $\rho$ tells you ``which checkpoint over the entire sweep behaves better''; the partial $\rho$ tells you whether a diagnostic retains a residual checkpoint-quality signal after removing the monotone trend associated with \stdmax{}. The two questions are distinct, and we will see in \Cref{sec:diag-cross} that they have markedly different answers for the same metric. A larger cross-architecture protocol (varying the latent geometry of the world model itself) is left to follow-up work with external baselines.

%% ===========================================================================
\section{Experiments}\label{sec:exp}

\subsection{Setup}\label{sec:exp-setup}

\paragraph{Tasks.} PushT (2D pushing), TwoRoom (2D navigation), Reacher (2D arm), Cube (3D manipulation).

\paragraph{Baselines.} LeWM-base (no noise) and LeWM+noise (8-level sweep).

\paragraph{Checkpoints.} The 36 checkpoints in this paper correspond to one trained model per $(\text{task}, \stdmax{})$ configuration.

\paragraph{Evaluation seeds.} Each checkpoint is evaluated with 3 evaluation seeds (42 / 43 / 44), with 100 trajectories per seed.

\paragraph{Evaluation protocol.} All success rates in this paper --- clean and noised, across all 36 checkpoints (4 tasks $\times$ \{base, \stdmax{} $0.001$--$0.008$\}) --- are computed under a single protocol: $n = 3$ seeds (42/43/44), \code{num\_eval = 100} trajectories per seed ($300$ trajectories per condition per checkpoint). Every cell of Tables~\ref{tab:ood-cliff},~\ref{tab:sweep-clean},~\ref{tab:sweep-px08} is mean $\pm$ across-seed population std over $n = 3$ and is sourced from the released aggregate, \dataset{canonical\_evals\_20260517.json}; raw per-seed files live alongside each checkpoint as \codebrk{<ckpt>/eval\_results/<cond>\_seed\{42,43,44\}\_metrics.txt}. The diagnostic source-of-truth for Figure~\ref{fig:scatter}, Table~\ref{tab:corr-n9}, Table~\ref{tab:partial-corr-4tasks}, and Table~\ref{tab:partial-corr} is \dataset{canonical\_diagnostics\_20260517.json}.

\paragraph{Hardware.} Single NVIDIA H800 (80\,GB).

\paragraph{Figures.} Six main figures (\Cref{fig:hero,fig:sweep,fig:pareto,fig:radar,fig:scatter,fig:mechanism}) are produced by \code{tools/paper1\_figs.py}.

\subsection{JEPA control fragility under visual OOD}\label{sec:exp-cliff}

\Cref{tab:ood-cliff} reports LeWM-base success rates under clean and noised evaluation (mean $\pm$ std across 3 seeds $\times$ 100 evaluations).

\begin{table}[H]
\centering
\caption{LeWM-base under visual OOD (mean $\pm$ std; 3 seeds $\times$ 100 evaluations).}
\label{tab:ood-cliff}
\small
\begin{tabular}{lrrrr}
\toprule
Task & clean & px+goal 0.05 & px+goal 0.08 & clean $\to$ 0.08 drop \\
\midrule
TwoRoom & $94.00 \pm 3.56$ & $61.33 \pm 5.31$ & $50.00 \pm 1.41$ & $\mathbf{-44.00}$ \\
PushT   & $86.33 \pm 2.36$ & $12.00 \pm 4.55$ & $\phantom{0}4.67 \pm 2.05$ & $\mathbf{-81.67}$ \\
Reacher & $58.67 \pm 1.25$ & $27.00 \pm 5.10$ & $15.00 \pm 2.16$ & $\mathbf{-43.67}$ \\
Cube    & $66.67 \pm 2.62$ & $53.33 \pm 3.30$ & $46.33 \pm 3.68$ & $\mathbf{-20.33}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{fig1_hero.png}
\caption{Visual OOD cliff in LeWM and recovery by noise training (success rate, 4 tasks). Blue: LeWM-base clean. Red: LeWM-base under px+g 0.08 noise. Green: the px+g 0.08 point-best LeWM+noise configuration. Annotations show base drop and recovery in points; $\sigma^\ast$ marks each task's px+g 0.08 point-best \stdmax{}.}
\label{fig:hero}
\end{figure}

LeWM-base is strong on clean images (especially TwoRoom and PushT), but visual std $= 0.05$ applied to pixels and goal jointly already produces large drops on all tasks: PushT loses $74+$ pts (down to near-random $4.67\%$ at std $= 0.08$), TwoRoom $30+$ pts, Reacher $30+$ pts, Cube $\sim 13$ pts (at std $= 0.05$). \textbf{This is not a marginal phenomenon}: a JEPA + CEM world model without noise-aware training has essentially no resistance to visual corruption. The drop pattern across tasks is informative: Cube degrades least ($-20.33$\,pt at std $= 0.08$) --- structured manipulation has some natural robustness --- while PushT degrades most catastrophically ($-81.67$\,pt), confirming that contact-heavy continuous control is most sensitive to visual precision.

As an external-baseline sanity check, a clean-trained PLDM checkpoint on PushT under the same 3-evaluation-seed $\times$ 100-trajectory protocol drops from $75.33 \pm 3.68$ clean to $43.67 \pm 4.64$ at px+goal 0.05 and $10.00 \pm 2.16$ at px+goal 0.08 (\Cref{sec:appendix-F}). This supports the narrow statement that the control-time visual-noise cliff is not isolated to LeWM. It does \emph{not} establish that the noise-training trade-off or the LeWM diagnostic correlations generalize across architectures; those require the ongoing PLDM/DINO-WM sweeps.

\subsection{Noise augmentation closes the gap --- at the cost of task-specific tuning}\label{sec:exp-sweep}

\Cref{tab:sweep-clean,tab:sweep-px08} report the complete 8-level sweep across tasks. All cells are success rate $\pm$ across-seed std (in pts), aggregated under the unified $n = 3$ seeds (42/43/44) $\times$ 100 trajectories protocol --- $300$ trajectories per cell.

\begin{table}[H]
\centering
\caption{LeWM+noise sweep, \textbf{clean} success rate (\%).}
\label{tab:sweep-clean}
\small
\begin{tabular}{lrrrr}
\toprule
\stdmax{} & TwoRoom & PushT & Reacher & Cube \\
\midrule
0 (base) & $94.00 \pm 3.56$ & $86.33 \pm 2.36$ & $58.67 \pm 1.25$ & $66.67 \pm 2.62$ \\
0.001 & $93.67 \pm 3.30$ & $88.00 \pm 3.74$ & $61.67 \pm 2.49$ & $69.33 \pm 0.47$ \\
0.002 & $95.00 \pm 2.83$ & $88.33 \pm 2.62$ & $85.67 \pm 2.49$ & $60.00 \pm 1.63$ \\
0.003 & $96.33 \pm 3.30$ & $89.67 \pm 1.70^{\dagger}$ & $78.67 \pm 1.25$ & $65.00 \pm 1.63$ \\
0.004 & $96.33 \pm 2.05$ & $89.33 \pm 2.05$ & $84.00 \pm 2.94$ & $69.00 \pm 3.74$ \\
0.005 & $96.00 \pm 2.83$ & $80.67 \pm 4.78$ & $70.00 \pm 2.16$ & $59.33 \pm 0.94$ \\
0.006 & $96.67 \pm 2.05$ & $89.33 \pm 2.05$ & $86.00 \pm 2.94^{\dagger}$ & $66.67 \pm 2.05$ \\
0.007 & $96.00 \pm 1.63$ & $85.67 \pm 3.09$ & $83.67 \pm 3.30$ & $67.67 \pm 0.94$ \\
0.008 & $98.33 \pm 0.47^{\dagger}$ & $88.33 \pm 2.87$ & $84.00 \pm 0.82$ & $62.33 \pm 1.25$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{LeWM+noise sweep, \textbf{px+goal 0.08} success rate (\%).}
\label{tab:sweep-px08}
\small
\begin{tabular}{lrrrr}
\toprule
\stdmax{} & TwoRoom & PushT & Reacher & Cube \\
\midrule
0 (base) & $50.00 \pm 1.41$ & $\phantom{0}4.67 \pm 2.05$ & $15.00 \pm 2.16$ & $46.33 \pm 3.68$ \\
0.001 & $87.67 \pm 1.89$ & $43.33 \pm 3.09$ & $46.00 \pm 1.63$ & $51.33 \pm 5.79$ \\
0.002 & $93.33 \pm 0.94$ & $71.33 \pm 3.68$ & $85.67 \pm 1.70^{\ddagger}$ & $60.67 \pm 0.47$ \\
0.003 & $94.67 \pm 2.87$ & $83.00 \pm 3.74$ & $73.67 \pm 0.47$ & $67.33 \pm 1.89$ \\
0.004 & $95.00 \pm 2.45$ & $81.33 \pm 2.87$ & $80.00 \pm 1.41$ & $67.00 \pm 3.56$ \\
0.005 & $95.67 \pm 2.36$ & $75.00 \pm 6.48$ & $68.00 \pm 3.56$ & $59.67 \pm 2.05$ \\
0.006 & $96.67 \pm 2.49$ & $87.00 \pm 3.74^{\ddagger}$ & $84.67 \pm 4.03$ & $65.00 \pm 2.94$ \\
0.007 & $96.33 \pm 2.05$ & $82.33 \pm 4.64$ & $81.33 \pm 1.25$ & $68.00 \pm 1.41^{\ddagger}$ \\
0.008 & $98.67 \pm 0.94^{\ddagger}$ & $85.33 \pm 2.62$ & $83.00 \pm 4.32$ & $60.33 \pm 0.94$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Notation.} $\dagger$ marks the clean point-best in that task column; $\ddagger$ marks the px+goal 0.08 point-best. Table~\ref{tab:diag-base-vs-best} and \Cref{fig:radar} use a separate term, \emph{representative diagnostic checkpoint}, for the checkpoint on which the full diagnostic suite was executed. Because many neighbouring settings differ by $\le 3$\,pts and overlap in across-seed std, we treat the optima as plateaus unless the gap is clearly larger than the seed-level variability.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig2_sweep.png}
\caption{Noise-training sweep: clean vs OOD per task. Dashed vertical lines mark each task's px+g 0.08 point-best $\sigma^\ast$. No single \stdmax{} is jointly optimal across tasks.}
\label{fig:sweep}
\end{figure}

Three observations follow.

\paragraph{(1) No single \stdmax{} is jointly optimal; clean and robust optima can dissociate within a task.}
TwoRoom peaks globally at \stdmax{} $= 0.008$ ($98.33$ / $98.67$); clean rises monotonically with noise. PushT peaks on clean at \stdmax{} $= 0.003$ but on robustness at \stdmax{} $= 0.006$ ($87.00$ vs.\ $0.002$'s $71.33$; $+15.67$ pt). Reacher lies on a $0.002$--$0.006$ plateau: clean point-best is at \stdmax{} $= 0.006$ ($86.00$), while px+g 0.08 point-best is at \stdmax{} $= 0.002$ ($85.67$). At \stdmax{} $= 0.001$ clean is $61.67$ vs base $58.67$ --- within across-seed std ($\sim 2.5$ pts), so the data support \emph{``low noise is statistically equivalent to base''}, not ``low noise hurts''. Cube responds least: clean is non-monotonic, and px+g 0.08 improves only in the $0.003$--$0.007$ range.

\paragraph{(2) Per-task tuning is necessary, not optional.}
Optimal \stdmax{} varies substantially: TwoRoom clean/OOD point-best $0.008$, PushT clean point-best $0.003$ / px+g 0.08 point-best $0.006$, Reacher clean point-best $0.006$ / px+g 0.08 point-best $0.002$, Cube px+g 0.08 point-best $0.007$ with a shallow clean plateau around $0.001$ / $0.004$ / $0.007$. Global input-side noise is the strongest ``global'' form of invariance pressure, but closing the OOD gap requires per-task tuning cost.

\paragraph{(3) Tasks form a clear sensitivity ordering at the extremes.}
PushT is clearly most sensitive ($-81.67$ base drop), Cube least sensitive ($-20.33$), while TwoRoom ($-44.00$) and Reacher ($-43.67$) are effectively tied around a $44$-pt drop. Recovery does not scale with sensitivity: TwoRoom recovers most fully ($+48.67$ pt at $\stdmax = 0.008$), Cube least ($+21.67$ pt). Input-side global noise is most effective on visually redundant tasks and gives diminishing returns on structured manipulation.

Plotting the same sweep as a (clean, OOD) trajectory per task (\Cref{fig:pareto}) makes the trade-off visually explicit: each task's sweep curve moves from base far below the $y = x$ diagonal toward the upper-right, with per-task curvature determined by how much OOD gain a task can buy from a given drop in clean.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{fig3_pareto.png}
\caption{Per-task Pareto trajectory of (clean, OOD) under noise sweep. Open marker $=$ base; solid markers $=$ \stdmax{} sweep (colour-by-\stdmax{}); ringed marker $=$ px+g 0.08 point-best. Dashed line $y = x$.}
\label{fig:pareto}
\end{figure}

\subsection{Diagnostic analysis: why global noise is not a silver bullet}\label{sec:exp-diag}

\Cref{tab:diag-base-vs-best} compares core diagnostic metrics on LeWM-base versus the per-task representative noise-trained diagnostic checkpoint.

\begin{table}[H]
\centering
\caption{Representation diagnostics: LeWM-base vs.\ a representative noise-trained diagnostic checkpoint per task. The $\sigma$ pick per task (0.008 / 0.002 / 0.006 / 0.001) is the ckpt on which the diagnostic suite was originally executed. Under the unified 3-seed $\times$ 100 eval protocol the PushT clean point-best shifts to \stdmax{} $= 0.003$ (within $\pm 2$\,pt of \stdmax{} $= 0.002$); we keep the diagnostics on the \stdmax{} $= 0.002$ checkpoint because the compression-vs.-resolution pattern this table illustrates is robust within this neighbourhood.}
\label{tab:diag-base-vs-best}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrrrr}
\toprule
Metric & TwoRoom & TwoRoom & PushT & PushT & Reacher & Reacher & Cube & Cube \\
       & base & noise (0.008) & base & noise (0.002) & base & noise (0.006) & base & noise (0.001) \\
\midrule
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.0281 & 0.2360 & 0.1051 & 0.0633 & 0.0676 & 0.1856 & 0.1879 \\
\code{clean\_effective\_rank}       & 47.60  & 33.59  & 76.42  & 42.85  & 61.04  & 65.92  & 73.25  & 71.83  \\
\transres{}\_l2  & 0.7216 & 0.6055 & 0.3015 & 0.2800 & 0.3704 & 0.3791 & 0.4847 & 0.4629 \\
\transres{}\_cos & 0.5538 & 0.3780 & 0.0868 & 0.0800 & 0.1351 & 0.1399 & 0.2347 & 0.2168 \\
\code{id\_probe\_r2}            & 0.2889 & $-0.0573$ & 0.7739 & 0.7500 & 0.1621 & 0.1729 & 0.6657 & 0.6720 \\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.4482 & 0.1283 & 0.1200 & 0.2518 & 0.2585 & 0.2364 & 0.2320 \\
\code{predictor\_rollout\_T8\_l2}      & 18.62  & 17.90  & 18.65  & 16.50  & 15.17  & 0.44   & 20.20  & 19.25  \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textbf{Notes.} (i) the TwoRoom transition-resolution values (L2 and cos) are corrected to the values directly stored in the released geometry-summary and task-resolution JSONs; (ii) Reacher and Cube representative diagnostics are taken from the corresponding checkpoint's per-tool JSON outputs under the diagnostics directory (max-std $= 0.1$, history-only noise); (iii) the Reacher representative rollout drift of $0.44$ (a 35$\times$ reduction from base $15.17$) is not a recording error: Reacher's representative checkpoint trains under \stdmax{} $= 0.006$, which is enough to collapse the multi-step predictor drift while keeping single-step task-resolution metrics flat --- precisely the dissociation that motivates the analysis in Section~\ref{sec:diag-cross}. Cube's representative drift ($19.25 \approx$ base $20.20$) is the opposite extreme.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{fig4_radar.png}
\caption{Per-task diagnostic radar: base vs representative noise-trained diagnostic checkpoint on 6 metrics, min-max normalised across all 4 tasks.}
\label{fig:radar}
\end{figure}

\paragraph{Mechanistic reading.}
\begin{itemize}
  \item \textbf{TwoRoom.} Low-dimensional, discrete, visually redundant. Compressing the representation (effective rank $47.6 \to 33.6$) is acceptable and even beneficial: a more compact latent space is easier to plan in.
  \item \textbf{PushT.} Continuous contact requires fine-grained pose resolution. Even at the representative diagnostic checkpoint (\stdmax{} $= 0.002$), the L2 transition-resolution metric already trends slightly downward. At heavier noise (e.g.\ $0.006$) it drops further, erasing contact-transition keyframes.
  \item \textbf{Reacher.} Low-dimensional continuous reaching does not show a resolution collapse in Table~\ref{tab:diag-base-vs-best}: effective rank, transition resolution, and the controllability probe remain flat or slightly improve. The notable change is the large reduction in multi-step predictor drift ($15.17 \to 0.44$), matching the later finding that Reacher's residual OOD-drop signal lives in the rollout metric rather than in the single-step \fragility{}.
  \item \textbf{Cube.} Cube is moderately resolution-demanding but less fragile than PushT. The representative checkpoint leaves rank, transition resolution, controllability probe, and rollout drift almost unchanged, consistent with Cube's smaller visual-OOD cliff and the moderate residual signal of multi-step drift in Table~\ref{tab:partial-corr-4tasks}.
  \item \textbf{Predictor-rollout caveat.} A drop in multi-step predictor rollout drift is not unambiguously good news. It can also mean the latent has become more \emph{predictable} without being more \emph{controllable}: predictor stability bought by sacrificing resolution.
\end{itemize}

\subsection{Cross-checkpoint correlation analysis}\label{sec:diag-cross}

\Cref{tab:corr-n9} reports task-specific cross-checkpoint correlations on the LeWM $n = 9$ sweep using the unified 3-seed $\times$ 100 evaluation.

We analyse two single-value-per-checkpoint diagnostic metrics that have full coverage on all 9 checkpoints per task: the \fragility{}, defined in Section~\ref{sec:bg-five-layers} as the single-step predictor target shift normalised by the nearest-neighbour cosine distance at the diagnostic's max-std injection, and the corresponding multi-step rollout L2 drift. Both are released in \dataset{canonical\_diagnostics\_20260517.json}, derived from each checkpoint's predictor-sensitivity diagnostic JSON, and are pure checkpoint-level quantities (independent of the evaluation protocol).

\begin{table}[H]
\centering
\caption{LeWM $n = 9$ sweep: per-task Pearson $r$ / Spearman $\rho$ vs OOD drop (clean $-$ px+g $0.08$). Eval values come from the unified 3-seed $\times$ 100 protocol.}
\label{tab:corr-n9}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrr}
\toprule
Metric & TwoRoom ($r$/$\rho$) & PushT ($r$/$\rho$) & Reacher ($r$/$\rho$) & Cube ($r$/$\rho$) \\
\midrule
\fragmetric{}                                & $+0.96$/$+0.72$ & $\mathbf{-0.54}$/$\mathbf{-0.77}$ & $+0.97$/$+0.35$ & $+0.36$/$+0.21$ \\
\code{predictor\_rollout\_T8\_l2\_at\_max\_std} & $+0.89$/$+1.00$ & $\mathbf{+0.99}$/$\mathbf{+0.88}$ & $\mathbf{+0.99}$/$\mathbf{+0.87}$ & $+0.92$/$+0.54$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\paragraph{Reading.} Unconditional correlations are large because both diagnostic metrics and the OOD drop are driven by the same upstream variable --- \stdmax{}. The relevant test is partial correlation conditioned on \stdmax{} (\Cref{tab:partial-corr-4tasks}).

\begin{table}[H]
\centering
\caption{Partial Spearman $\rho(\text{metric},\,\text{OOD drop} \mid \stdmax{})$, $n = 9$ per task.}
\label{tab:partial-corr-4tasks}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrr}
\toprule
Metric & TwoRoom & PushT & Reacher & Cube \\
\midrule
\fragmetric{}                                & --- (rank ties) & $+0.06$ & $-0.12$ & $+0.14$ \\
\code{predictor\_rollout\_T8\_l2\_at\_max\_std} & --- (rank ties) & $-0.03$ & $\mathbf{+0.79}$ & $+0.50$ \\
\bottomrule
\end{tabular}%
}
\end{table}

The TwoRoom partial estimates are undefined because saturating success rates produce rank ties that make residualisation singular at $n = 9$. PushT, Reacher and Cube give clean readings: after removing the monotone sweep trend associated with \stdmax{}, the \textbf{fragility metric carries essentially no information about the size of the OOD drop} on any task; the \textbf{multi-step predictor drift} still carries signal on Reacher ($+0.79$) and weak signal on Cube ($+0.50$). The Reacher partial $\rho$ is the only non-trivial residual correlation in the entire matrix.

\paragraph{PushT $n = 9$ detailed view (\fragility{}).}

\begin{table}[H]
\centering
\caption{Spearman $\rho$ (unconditional) and partial Spearman $\rho$ conditioned on \stdmax{}, for the \fragility{} on the PushT $n = 9$ LeWM sweep under the unified 3-seed $\times$ 100 eval.}
\label{tab:partial-corr}
\small
\begin{tabular}{lr}
\toprule
Quantity & Spearman $\rho$ \\
\midrule
$\rho(\stdmax{}, \text{metric})$ & $+0.83$ \\
$\rho(\stdmax{}, \text{clean})$ & $0.00$ \\
$\rho(\stdmax{}, \text{px+goal } 0.08)$ & $+0.82$ \\
$\rho(\stdmax{}, \text{OOD drop})$ & $-0.93$ \\
\midrule
$\rho(\text{metric}, \text{clean})$ unconditional & $-0.33$ \\
$\rho(\text{metric}, \text{clean}) \mid \stdmax{}$ partial & $\mathbf{-0.59}$ \\
$\rho(\text{metric}, \text{px+g } 0.08)$ unconditional & $+0.55$ \\
$\rho(\text{metric}, \text{px+g } 0.08) \mid \stdmax{}$ partial & $\mathbf{-0.41}$ \\
$\rho(\text{metric}, \text{OOD drop})$ unconditional & $-0.77$ \\
$\rho(\text{metric}, \text{OOD drop}) \mid \stdmax{}$ partial & $+0.06$ \\
\bottomrule
\end{tabular}
\end{table}

Interpretation:
\begin{enumerate}
  \item \textbf{After removing the monotone trend associated with \stdmax{}, the metric still carries a meaningful residual checkpoint-quality signal.} Partial $\rho$ on clean is $-0.59$ and on px+goal 0.08 is $-0.41$ --- both negative, both meaningful at this sample size. On the $n = 9$ PushT sweep, lower \fragility{} aligns with better clean \emph{and} better noisy success beyond what the sweep-level \stdmax{} trend already explains.
  \item \textbf{The metric does NOT predict the clean--OOD gap.} The unconditional $\rho(\text{metric}, \text{drop}) = -0.77$ looks impressive, but partialling out \stdmax{} collapses it to $+0.06$ (sign-flipped, near zero). The drop's strong correlation with the metric is a mediated effect: \stdmax{} drives both the metric ($\rho = +0.83$) and the drop ($\rho = -0.93$). After the \stdmax{} trend is removed, the metric tells you nothing new about how much the \emph{gap} between clean and noisy performance will be.
  \item \textbf{Note the unconditional-vs-partial sign reversal on clean.} $\rho(\text{metric}, \text{clean})$ is only $-0.33$ unconditionally --- clean success rate is roughly flat across the PushT sweep under the 3-seed protocol (range $80.67$--$89.67$), so \stdmax{} barely moves clean. The partial correlation \emph{strengthens} to $-0.59$ once the sweep-level \stdmax{} trend is removed, exactly because the residual signal is what the metric tracks.
  \item \textbf{Practical reading.} The toolkit is a model-selection tool that ranks checkpoints after controlling for the sweep-level \stdmax{} effect. It is \emph{not} a substitute for actually training with noise when the OOD gap is the quantity of interest.
\end{enumerate}

\subsubsection{Clean control fidelity and OOD robustness as separable signals}\label{sec:diag-clean-vs-ood}

The partial-correlation analysis above settles the interpretation:

\begin{itemize}
  \item The metric is a \textbf{residual checkpoint-quality signal beyond the sweep-level \stdmax{} effect} --- a lower \fragility{} means better control on \emph{both} clean and noisy evaluation (partial $\rho = -0.59$ / $-0.41$ on PushT).
  \item The metric \textbf{does not isolate noise robustness} --- its apparent correlation with the gap between clean and noisy success is fully mediated by \stdmax{} (partial $\rho = +0.06$).
\end{itemize}

The PushT scatter (\Cref{fig:scatter}) makes both halves visible. Panel (a) plots metric vs clean (unconditional Spearman $\rho = -0.33$; the residual trend after conditioning on \stdmax{} is what the partial correlation captures). Panel (b) plots metric vs OOD drop (unconditional $\rho = -0.77$), but the colour-bar (encoding \stdmax{}) reveals the structure: low-\stdmax{} checkpoints (light blue) live at high drop, high-\stdmax{} checkpoints (dark blue) live at low drop, and the metric tracks \stdmax{} itself. After the \stdmax{} trend is removed, the metric does not separate small-drop from large-drop checkpoints.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig5_scatter.png}
\caption{PushT $n = 9$ LeWM sweep: the \fragility{} is a checkpoint-quality predictor (a); the apparent OOD-drop correlation in (b) is mediated by \stdmax{} (encoded by the colour bar).}
\label{fig:scatter}
\end{figure}

\subsection{Mechanism attribution: where in the pipeline does noise cause failure?}\label{sec:exp-mech}

\Cref{sec:exp-diag} reports \emph{what} is compressed and \Cref{sec:diag-cross} reports \emph{which diagnostics} correlate with eval across checkpoints, but neither answers \textbf{``is the failure in the encoder, the predictor, or the cost surface?''} We address this with two complementary experiments.

\subsubsection{Auxiliary cost-swap sanity check: cost alone is unlikely to explain the collapse}\label{sec:mech-cost-swap}

We ran a one-off eval-only cost swap on a single TwoRoom checkpoint outside the 36-checkpoint canonical evaluation table; full details are reported in \Cref{sec:appendix-E}. Switching the CEM cost from cosine/normalized to mse/raw changes px+goal 0.03 success only from 36.0 to 42.0, still far below a separate clean reference of 69.7. As a sanity check, this suggests that the cost function alone is unlikely to explain the OOD collapse.

\subsubsection{Latent-noise probing: encoder is the principal bottleneck}\label{sec:mech-latent-probing}

Directly injecting noise into the latent $z$ (skipping the encoder) decouples encoder contributions from predictor + cost. Comparing diagnostic signals across two injection points (pixels vs.\ latent $z$, both history-only noise at max std):

\begin{itemize}
  \item \textbf{PushT.} The multi-step input-space metric \code{predictor\_rollout\_T8\_l2\_at\_max\_std} has unconditional $\rho = +0.88$ vs OOD drop (\Cref{tab:corr-n9}), driven primarily by \stdmax{}; after removing the monotone \stdmax{} trend, the partial $\rho$ collapses to $-0.03$. The single-step \fragility{} (unconditional $\rho = -0.77$; partial $+0.06$ after removing the same trend) tells the same story. Both encoder--predictor signals are mediated by training noise; once that sweep-level effect is accounted for, neither isolates the OOD drop on PushT.
  \item \textbf{Reacher.} \code{predictor\_rollout\_T8\_l2\_at\_max\_std} has unconditional $\rho = +0.87$ \emph{and} partial $\rho = \mathbf{+0.79}$ vs OOD drop --- the only non-trivial residual correlation in the matrix. On Reacher, multi-step predictor drift carries genuine OOD-drop information beyond \stdmax{}.
  \item \textbf{Cube.} \code{predictor\_rollout\_T8\_l2\_at\_max\_std} has unconditional $\rho = +0.54$ and partial $\rho = +0.50$ --- a moderate residual signal. Encoder--predictor drift partially explains Cube's small but non-zero OOD sensitivity.
  \item \textbf{TwoRoom.} Partial correlations are undefined because clean / drop are saturated across the sweep (rank ties). Unconditional $\rho$ still indicates strong encoder--predictor sensitivity to \stdmax{}.
\end{itemize}

\Cref{tab:attribution} summarises the three-layer attribution.

\begin{table}[H]
\centering
\caption{Three-layer attribution by task (LeWM $n = 9$ sweep, unified 3-seed $\times$ 100 eval).}
\label{tab:attribution}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabularx}{\textwidth}{l X X}
\toprule
Task & Primary cause & Residual after removing the \stdmax{} trend \\
\midrule
TwoRoom & encoder dominant (rank-saturated) & n/a \\
PushT   & encoder + single-step predictor (both mediated by \stdmax{}) & no residual metric isolates OOD drop \\
Reacher & encoder + multi-step rollout & multi-step drift residual signal (partial $\rho = +0.79$) \\
Cube    & encoder & moderate residual on multi-step drift (partial $\rho = +0.50$) \\
\bottomrule
\end{tabularx}
\end{table}

The common primary cause across the four tasks is \textbf{encoder shift transduced by the predictor}. The auxiliary cost-swap sanity check in \Cref{sec:mech-cost-swap} suggests that the cost function alone is unlikely to explain the collapse, but we do not treat that single-checkpoint ablation as a task-wide quantitative attribution. This is also the root reason Layer-5 task-resolution metrics carry strong signal in \Cref{sec:exp-diag}: once the encoder's latent-neighbourhood structure is corrupted beyond the NN distance scale, downstream predictor and planner are already operating on the wrong neighbourhood.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig6_mechanism.png}
\caption{Mechanism schematic. Pixels perturb the encoder, the predictor transduces that shift into planning-relevant latent drift, and CEM acts on the resulting latent geometry. Quantitative attribution in \S\ref{sec:mech-latent-probing} uses the two full-coverage LeWM $n = 9$ predictor metrics; after conditioning on \stdmax{}, Reacher's multi-step drift is the only non-trivial residual signal.}
\label{fig:mechanism}
\end{figure}

%% ===========================================================================
\section{Discussion}\label{sec:disc}

\subsection{Rethinking the ``JEPA invariance'' narrative}\label{sec:disc-narrative}

The data challenge an implicit community assumption: latent prediction alone is not sufficient to confer invariance to visual noise. The invariance a JEPA encoder acquires is \emph{in-distribution invariance} --- invariance to visual patterns present in the training distribution. When test-time inputs carry high-frequency pixel noise not seen during training, the topology of the latent space breaks down, the predictor outputs incorrect future states, and the planner fails.

This stands in contrast to VJEPA~\cite{huang2026vjepa}, which reports $R^2 > 0.84$ under a Noisy-TV distractor --- but that result is on 1D synthetic signals evaluated by linear probe. \textbf{Control-task evaluation (success rate) is a strictly stricter standard than linear-probe $R^2$}: linear probes only need enough information for a linear classifier to extract, whereas control tasks demand a representation that supports precise latent-space planning and action optimisation.

\subsection{The invariance--resolution trade-off --- manifestation and scope}\label{sec:disc-scope}

The trade-off has the following four-task signature in this paper:

\begin{itemize}
  \item \textbf{TwoRoom.} Background wall colour / texture is pure visual redundancy; discarding it does not impair navigation.
  \item \textbf{PushT.} The pixel changes during T-block / arm contact carry critical force and pose information; discarding them blinds the planner.
  \item \textbf{Reacher.} Low-dimensional continuous control; visual encoding of joint angle requires moderate resolution.
  \item \textbf{Cube.} Object pose and grasp-point spatial relations need moderate resolution, but Cube itself is largely insensitive to global noise (action sequence is structured; visual--action coupling is predictable).
\end{itemize}

Task-specificity explains why there is no single optimal noise level.

\paragraph{Scope.} Whether the trade-off generalises to other latent world-model architectures is an open question we do not address. Reconstruction-based world models such as DreamerV3~\cite{hafner2025dreamerv3} explicitly model observations, while decoder-free latent MPC systems such as TD-MPC2~\cite{hansen2024tdmpc2} may exhibit different compression dynamics. ViGMO~\cite{vigmo} observes related task-specific noise sensitivity on DMC. EMA-target JEPA (I-JEPA / V-JEPA lineage) and variational JEPA may exhibit different dynamics. This paper's scope is LeWM + CEM; universalising the trade-off exceeds the present evidence.

\subsection{When the diagnostic toolkit works --- and when it does not}\label{sec:disc-boundary}

Three scope conditions should be stated up-front so practitioners know when to use the toolkit and when to fall back to direct evaluation.

\paragraph{Scope 1: the toolkit ranks residual checkpoint quality, not OOD-specific robustness.}
\Cref{sec:diag-clean-vs-ood} shows that on PushT, after conditioning on \stdmax{}, the strongest cross-checkpoint diagnostic (the \fragility{}) has partial Spearman $\rho = -0.59$ with clean success and $\rho = -0.41$ with px+goal 0.08 success. It does \emph{not} isolate OOD-specific robustness: the partial correlation with the clean-to-OOD drop is only $+0.06$. Use the toolkit to pick the strongest checkpoint after removing the sweep-level \stdmax{} trend; do not use it as a substitute for an actual OOD eval if your downstream question is robustness.

\paragraph{Scope 2: tasks with weak per-state controllability variance fall outside the toolkit's strict regime.}
Reacher and TwoRoom do not produce a diagnostic-vs-eval Spearman $\rho$ that survives the partial-correlation criterion. Both tasks lack enough residual spread after accounting for \stdmax{} to distinguish ``good'' from ``bad'' checkpoints via a label-free metric. The toolkit can describe what these models have compressed (\Cref{tab:diag-base-vs-best}) but cannot predict relative checkpoint quality.

\paragraph{Scope 3: cross-checkpoint diagnostics cannot recover what training did not provide.}
The largest determinant of OOD drop is whether the model saw noise during training --- not which \fragility{} it happens to score on. This is the structural reason $\rho(\text{metric}, \text{drop})$ is weak even when $\rho(\text{metric}, \text{clean})$ is strong: noise vs no-noise training puts each checkpoint on a completely different (clean, OOD) curve (cf.\ \Cref{fig:pareto}), and no static cross-checkpoint diagnostic can substitute for that training-time choice.

The toolkit therefore has a precise scope: it is a \emph{clean-evaluation auxiliary} that lets you select among checkpoints on tasks with strong per-state controllability variance (PushT, Cube), assuming the training protocol is already fixed. It is not an OOD prediction oracle.

\subsection{Practical guidance: how to choose \stdmax{} on a new task}\label{sec:disc-practical}

Our sweep suggests a simple operational recipe:
\begin{enumerate}
  \item Inspect the clean baseline's \fragility{} as a screening diagnostic, not as an OOD oracle. Very small values (PushT / Cube regime) indicate that local encoder--predictor shift is small relative to the clean nearest-neighbour scale, but they do not by themselves rank OOD sensitivity.
  \item Check \code{clean\_effective\_rank}, \transres{}, and task semantics together. PushT combines high rank and high controllability (\idprobe{} $=0.7739$), so noise should be swept with clean performance as a guardrail. TwoRoom is visually redundant and retains high transition separability (\transres{}\_l2 $=0.7216$), so a wider sweep up to $0.008+$ is reasonable.
  \item Use \emph{two endpoints} in evaluation (clean and max-noise). Checking only one misses one of the optima: PushT's clean optimum at $0.003$ vs robustness optimum at $0.006$ is the clearest example.
  \item Under compute budget, start with a coarse 4-level sweep (\{0.001, 0.003, 0.005, 0.007\}), then refine around the best clean/OOD candidates. The coarse grid is a screening step, not a guarantee of locating the exact point-best \stdmax{}.
\end{enumerate}

\subsection{Limitations and future directions}\label{sec:disc-limit}

\paragraph{Limitation 1 --- Single backbone family.} The full sweep and diagnostic analysis are validated on LeWM. The PushT PLDM sanity check in \Cref{sec:appendix-F} shows a similar clean-trained visual-noise cliff, but it is not a cross-architecture sweep; other JEPA variants (EMA-target I-JEPA / V-JEPA lineage; variational JEPA) may exhibit different noise responses.

\paragraph{Limitation 2 --- Gaussian pixel noise only.} Real-world visual corruption includes motion blur, contrast variation, occlusion, and lighting change; whether the trade-off transfers is open.

\paragraph{Limitation 3 --- Diagnostic framework is empirical, not theoretical.} Reacher and TwoRoom fail the partial-correlation criterion for \emph{all} metrics, exposing where the empirical framework breaks down.

\paragraph{Limitation 4 --- Automated transition reweighting is not a substitute for noise training.} As a sanity check we also tested a scale-preserving heteroscedastic negative-log-likelihood (NLL) formulation in which a learned per-transition $\sigma$-head down-weights high-error transitions. The result is informative as a negative finding: TwoRoom clean reaches $99.67\%$ but high-noise robustness lags noise training; PushT clean \emph{collapses} from $86.33\%$ to $13.33\%$ because contact-control transitions have high prediction error and are exactly the transitions $\sigma$-weighting would discard. Full data are reported in \Cref{sec:appendix-D}. The lesson --- ``hard $\ne$ unimportant'' in contact control --- connects this paper's trade-off to the broader question of per-token controllers, which we leave to follow-up work.

\paragraph{Future direction 1 --- Per-token adaptive consistency.} \Cref{sec:diag-cross,sec:exp-mech} identify the strongest signal --- the \fragility{} --- as a checkpoint-level scalar. Whether its per-token variant can serve as a per-token consistency controller is an open methodological question, under investigation in follow-up work.

\paragraph{Future direction 2 --- Cross-architecture replication.} Running the sweep and diagnostic protocol on external world-model baselines would reveal whether the trade-off is JEPA-specific or shared by broader latent world-model families.

\paragraph{Future direction 3 --- Theoretical grounding.} Reformulating the trade-off in an information-bottleneck / rate-distortion language is an attractive direction; we did not pursue it because the empirical phenomenology may not yet be stable enough for formal modelling.

%% ===========================================================================
\section{Conclusion}\label{sec:conc}

This paper provides a systematic diagnostic study of the visual-OOD control robustness of JEPA + CEM world models, using LeWorldModel as the representative system. Three findings:

\begin{enumerate}
  \item Visual OOD collapse in JEPA + CEM control. Without noise-aware training, LeWM collapses under pixel noise; latent prediction alone does not confer visual robustness.
  \item Global noise augmentation has a boundary. It effectively closes the OOD gap but has no globally optimal level; task differences are large, and within a single task clean and robustness optima can dissociate.
  \item A five-layer diagnostic protocol reveals the underlying mechanism. Noise-augmentation gains arise from representation compression, but excessive compression destroys the resolution required for control --- the \emph{invariance--resolution trade-off}. When used cross-checkpoint, the strongest single diagnostic (the \fragility{}) predicts clean control quality rather than OOD-specific robustness; we delineate this scope explicitly.
\end{enumerate}

This paper does not propose a new training algorithm. Its contribution is a systematic body of empirical evidence, a reusable diagnostic toolkit, and an explicit delineation of what cross-checkpoint diagnostics can and cannot predict.

%% ===========================================================================
\section*{Acknowledgements}
The complete code and data are available at \url{https://github.com/qun-team/wm_exp}. We thank the LeWM authors for releasing the baseline framework on which this study builds.

%% ===========================================================================
\bibliographystyle{plain}
\bibliography{references}

%% ===========================================================================
\appendix
\section{Experimental details}\label{sec:appendix-A}

\subsection{Environments}
\begin{itemize}
  \item \textbf{PushT.} 2D continuous pushing; 20{,}000 expert episodes; $\sim$\,196 steps/episode; action dim 2 (direction + magnitude); 224$\times$224 RGB.
  \item \textbf{TwoRoom.} 2D continuous navigation; 10{,}000 episodes; $\sim$\,92 steps; action dim 2; 224$\times$224 RGB.
  \item \textbf{Reacher.} 2D arm control (DeepMind Control Suite); 10{,}000 episodes; 200 steps; action dim 2.
  \item \textbf{Cube.} 3D cube manipulation (OGBench); 10{,}000 episodes; 200 steps; action dim 7.
\end{itemize}

\subsection{Noise augmentation implementation}
The actual implementation is \code{utils.py::AddNormalizedGaussianNoise}. Key points: (i) noise is added on ImageNet-normalized tensors and must be divided by the per-channel std to retain ``pixel-space std'' semantics; (ii) sampling is per-frame independent over the leading frame dims, not per-batch; (iii) all sweeps in this paper fix \code{noise\_prob} $= 1.0$ and \code{std\_min} $= 0$ and vary only \stdmax{}.

\begin{lstlisting}
class AddNormalizedGaussianNoise:
    """Per-frame independent. Each frame draws Bernoulli(noise_prob) then
    std ~ Uniform(std_min, std_max). Pixel-space std is converted to
    normalized space by dividing by per-channel std before adding."""
    def __init__(self, std_min, std_max, noise_prob=1.0):
        self.std_low, self.std_high, self.noise_prob = std_min, std_max, noise_prob
        self.channel_std = torch.as_tensor(IMAGENET_STD)  # (C,)

    def __call__(self, x):
        if self.std_high <= 0 or self.noise_prob <= 0:
            return x
        leading = x.shape[:-3]
        stds = torch.empty(leading, device=x.device, dtype=x.dtype).uniform_(
            self.std_low, self.std_high)
        if self.noise_prob < 1.0:
            mask = (torch.rand(leading, device=x.device) < self.noise_prob).to(x.dtype)
            stds = stds * mask
        per_frame_scale = stds.view(*leading, 1, 1, 1)
        channel_factor = (1.0 / self.channel_std.to(x.device, x.dtype)).view(
            *([1] * len(leading)), -1, 1, 1)
        scale = per_frame_scale * channel_factor   # pixel-space std -> normalized
        return x + torch.randn_like(x) * scale
\end{lstlisting}

\subsection{Evaluation protocol}
Clean evaluation uses no noise, 100 trajectories per seed $\times$ 3 seeds (42/43/44), totalling 300 trajectories per condition. Noisy evaluation adds Gaussian noise of std $\in \{0.05, 0.08\}$ to both pixels and the goal image under the same 3-seed protocol. Success criterion is task-specific (PushT: T-block pose match; TwoRoom: target region; Reacher: joint-angle match; Cube: cube position match).

\subsection{Compute}
Training runs on a single NVIDIA H800 (80\,GB) and follows the LeWM training schedule released with the baseline.

\subsection{Main-figure rendering}
The six main figures are produced by \code{tools/paper1\_figs.py}; run \codebrk{python -m tools.paper1\_figs --out-dir assets/paper1\_figs}. The script reads \dataset{canonical\_evals\_20260517.json} together with \dataset{canonical\_diagnostics\_20260517.json}; no checkpoint or model loading is required.

%% ===========================================================================
\section{Full diagnostic profile of LeWM-base on four tasks}\label{sec:appendix-B}

\Cref{tab:full-diag} summarises every core diagnostic on the four LeWM-base checkpoints. Data are taken from each checkpoint's per-tool JSON outputs under \codebrk{eval\_results/diagnostics/} (the \code{geometry}, \code{task}, \code{predictor} and \code{latent\_noise} families).

\begin{table}[H]
\centering
\caption{Full LeWM-base diagnostics across four tasks.}
\label{tab:full-diag}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrr}
\toprule
Layer / Metric & TwoRoom & PushT & Reacher & Cube & Unit / note \\
\midrule
\multicolumn{6}{l}{\textit{Encoder geometry}}\\
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.2360 & 0.0633 & 0.1856 & cosine distance \\
\code{clean\_pair\_cos\_dist\_median} & 0.9904 & 1.0228 & 1.0252 & 1.0193 & pair-wise cos dist \\
\code{clean\_effective\_rank} & 47.60 & 76.42 & 61.04 & 73.25 & effective rank \\
\midrule
\multicolumn{6}{l}{\textit{Noise sensitivity}}\\
\code{noise\_angle\_deg\_median} (@\,std$=0.005$) & $5.51^{\circ}$ & $1.33^{\circ}$ & $3.22^{\circ}$ & $1.40^{\circ}$ & median angular shift \\
\code{noise\_to\_nn\_cos\_ratio\_median} & 0.1031 & 0.0011 & 0.0249 & 0.0016 & noise/NN cos ratio \\
\code{robust\_radius\_std} & 0.0142 & 0.0537 & 0.0142 & 0.0356 & critical noise std \\
\code{first\_risk\_std} & $>$0.08 & $>$0.08 & $>$0.08 & $>$0.08 & first high-risk std \\
\code{noise\_angle\_slope\_deg\_per\_std} & 1085.8 & 284.8 & 831.7 & 327.0 & $^{\circ}$/std angular gain \\
\code{geometry\_flag} & balanced & robust & balanced & robust & geometry label \\
\midrule
\multicolumn{6}{l}{\textit{Task resolution}}\\
\transres{}\_l2  & 0.7216 & 0.3015 & 0.3704 & 0.4847 & L2 resolution ratio \\
\transres{}\_cos & 0.5538 & 0.0868 & 0.1351 & 0.2347 & cos resolution ratio \\
\code{id\_probe\_r2} & 0.2889 & 0.7739 & 0.1621 & 0.6657 & linear probe $R^{2}$ \\
\code{id\_probe\_r2\_min} & 0.2599 & 0.6786 & 0.1366 & 0.0972 & min probe $R^{2}$ \\
\code{lidar\_rank} & 46.06 & 13.95 & 45.90 & 42.46 & LiDAR rank proxy \\
\midrule
\multicolumn{6}{l}{\textit{Action effect}}\\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.1283 & 0.2518 & 0.2364 & action-perturb mean shift \\
\code{action\_perturb\_pred\_shift\_corr} & 0.2847 & 0.2873 & 0.4042 & 0.2559 & shift $\times$ $\|a\|$ corr \\
\midrule
\multicolumn{6}{l}{\textit{Predictor stability}}\\
\code{predictor\_rollout\_T8\_l2} & 18.62 & 18.65 & 15.17 & 20.20 & T=8 rollout L2 drift \\
\fragmetric{} & 1.51e-4 & 3.54e-6 & 2.67e-5 & 3.39e-6 & max std target/NN ratio \\
\midrule
\multicolumn{6}{l}{\textit{Latent noise}}\\
\code{cka\_linear\_at\_max\_std} & 0.1986 & 0.5536 & 0.3085 & 0.1814 & CKA clean vs noisy \\
\code{latent\_cost\_surface\_slope\_z} & 635.31 & 1.3886 & 599.45 & 0.6208 & goal latent cost slope \\
\bottomrule
\end{tabular}%
}
\end{table}

%% ===========================================================================
\section{Heteroscedastic-loss formulation}\label{sec:appendix-C}

The scale-preserving heteroscedastic NLL referenced in \Cref{sec:disc-limit} (Limitation 4) and detailed in \Cref{sec:appendix-D} is:
\begin{equation}
  \mathcal{L}_{\text{hetero}} = \tfrac{1}{2}\,\exp(-s_t)\,\| z_{t+1} - \hat z_{t+1}\|^2 + \tfrac{1}{2}\, s_t \,,
  \label{eq:hetero}
\end{equation}
where $s_t$ is the $\sigma$-head-predicted log-variance. During training $\exp(-s_t)$ acts as an automatic weight: high-error transitions are down-weighted and low-error transitions are up-weighted. When $s_t \equiv 0$ the loss reduces to plain MSE.

%% ===========================================================================
\section{Heteroscedastic-loss negative result (data)}\label{sec:appendix-D}

This appendix records the full data behind \Cref{sec:disc-limit} Limitation 4. The $\sigma$-head is trained jointly with the predictor; gradient is detached on the $\sigma$ path so the mean prediction path is exactly LeWM MSE when $\sigma$ is constant.

\begin{table}[H]
\centering
\caption{Heteroscedastic-loss evaluation.}
\label{tab:hetero-eval}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrr}
\toprule
Task / model & Clean & goal 0.05 & pixels 0.05 & px+goal 0.05 & goal 0.08 & px+goal 0.08 \\
\midrule
TwoRoom LeWM-base       & 94.00 & 73.33 & 72.33 & 61.33 & 58.67 & 50.00 \\
TwoRoom LeWM+noise point-best & 98.33 & 98.00 & 98.33 & 98.00 & 98.67 & 98.67 \\
TwoRoom hetero          & \textbf{99.67} & 85.33 & 96.67 & 84.67 & 73.33 & 55.33 \\
PushT LeWM-base         & 86.33 & 38.00 & 17.00 & 12.00 & 11.33 &  4.67 \\
PushT LeWM+noise point-best   & \textbf{89.33} & 87.67 & 88.00 & 88.33 & 89.67 & 87.00 \\
\textbf{PushT hetero}   & \textbf{13.33} &  7.67 &  7.67 &  7.67 &  9.67 &  6.00 \\
\bottomrule
\end{tabular}%
}
\end{table}

TwoRoom hetero reaches $99.67\%$ clean --- consistent with the prior that low-dimensional discrete tasks benefit from stronger invariance / clustering --- but lags noise training on high-noise robustness. \textbf{PushT hetero clean is $13.33\%$ --- a method-level failure}, not a robustness trade-off.

\begin{table}[H]
\centering
\caption{Representation diagnostics of the hetero-loss failure.}
\label{tab:hetero-diag}
\small
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrr}
\toprule
Metric & TwoRoom base & TwoRoom hetero & PushT base & PushT hetero \\
\midrule
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.0281 & 0.2360 & 0.1051 \\
\code{clean\_effective\_rank}       & 47.60  & 33.59  & 76.42  & 42.85  \\
\transres{}\_cos & 0.5538 & 0.3780 & 0.0868 & 0.0101 \\
\transres{}\_l2 & 0.7216 & 0.6055 & 0.3015 & \textbf{0.1023} \\
\code{id\_probe\_r2}            & 0.2889 & $-0.0573$ & 0.7739 & \textbf{0.2678} \\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.4482 & 0.1283 & 0.0841 \\
\code{predictor\_rollout\_T8\_l2}      & 18.62  & 17.90  & 18.65  & 14.01  \\
\bottomrule
\end{tabular}%
}
\end{table}

Hetero-loss compresses the representation in both tasks. For TwoRoom (low-dimensional, discrete, redundant), compression is acceptable. For PushT, \transres{}\_l2 collapses from $0.3015$ to $0.1023$ and \idprobe{} from $0.7739$ to $0.2678$ --- task-relevant state information is erased. The drop in \code{predictor\_rollout\_T8\_l2} is not good news either: the latent has become more \emph{predictable} without being more \emph{controllable}.

\paragraph{Mechanism.} The $\sigma$-head correctly learns per-transition difficulty (high $\sigma$ on contact frames in PushT, on doorway crossings in TwoRoom, etc.), but using $\sigma$ as an automatic weight to down-weight high-error transitions misclassifies the contact-and-control-critical states of PushT as ``unimportant'' and erases them. This is a direct demonstration of the broader trade-off lesson: in contact-heavy control, \textbf{hard $\ne$ unimportant}.

This finding motivates a follow-up direction we are currently exploring: per-token \emph{consistency} (not loss-reweighting) controllers driven by detached difficulty signals, which preserve the mean prediction path's gradient distribution. That work is independent of this paper's diagnostic study and will be reported separately.

%% ===========================================================================
\section{One-off cost-swap sanity check}\label{sec:appendix-E}

This appendix records the eval-only cost-swap ablation referenced in \Cref{sec:mech-cost-swap}. It is \textbf{not} part of the 36-checkpoint canonical evaluation table and uses a separate clean reference ($\text{num\_eval} = 300$), so we report it only as a sanity check rather than a pooled statistic.

\begin{table}[H]
\centering
\caption{One-off eval-only cost swap on a TwoRoom checkpoint, std $= 0.03$ pixels+goal. Reference row reports a separate clean eval of the same checkpoint ($\text{num\_eval} = 300$).}
\label{tab:cost-swap}
\small
\begin{tabular}{llcr}
\toprule
Variant & Cost type & Cost space & Success rate \\
\midrule
A (default)           & cosine & normalized & 36.0 \\
B (swap)              & mse    & raw        & 42.0 \\
Clean reference (same ckpt) & ---    & ---        & 69.7 \\
\bottomrule
\end{tabular}
\end{table}

Swapping cost recovers only $+6$\,pt ($36 \to 42$), still far below the clean reference ($69.7$). The narrow reading is therefore: \textbf{a sanity check suggests the cost function alone is unlikely to explain the OOD collapse.}

%% ===========================================================================
\section{External baseline sanity check: clean-trained PLDM on PushT}\label{sec:appendix-F}

This appendix records the PushT PLDM baseline referenced in \Cref{sec:exp-cliff}. PLDM follows the joint-embedding predictive line from Sobal et al.~\cite{sobal2022jointembeddingpredictivearchitectures}; the concrete latent-dynamics planning baseline is from Sobal et al.~\cite{sobal2025stresstesting}. Our run uses the implementation distributed through the \code{stable-worldmodel} baseline suite~\cite{maes2026stableworldmodel}. The run is clean-trained (\code{image\_noise.std\_max = 0}, \code{noise\_prob = 0}) and evaluated with the same three evaluation seeds (42/43/44) and 100 trajectories per seed as the LeWM canonical tables. It is outside the 36-checkpoint LeWM sweep and is not used in \Cref{tab:corr-n9,tab:partial-corr-4tasks,tab:partial-corr}.

\begin{table}[H]
\centering
\caption{Clean-trained PLDM external-baseline sanity check on PushT. Values are success rate mean $\pm$ population std over 3 evaluation seeds $\times$ 100 trajectories.}
\label{tab:pldm-pusht}
\small
\begin{tabular}{lrrrr}
\toprule
Model & clean & px+goal 0.05 & px+goal 0.08 & clean $\to$ 0.08 drop \\
\midrule
LeWM-base & $86.33 \pm 2.36$ & $12.00 \pm 4.55$ & $\phantom{0}4.67 \pm 2.05$ & $-81.67$ \\
PLDM clean-trained & $75.33 \pm 3.68$ & $43.67 \pm 4.64$ & $10.00 \pm 2.16$ & $-65.33$ \\
\bottomrule
\end{tabular}
\end{table}

The result supports a limited external-baseline reading: clean-trained visual world models can share the same control-time pixel+goal noise failure mode. PLDM starts from a lower clean success than LeWM-base on PushT, but still loses $65.33$\,pt under px+goal 0.08. Its diagnostic profile also cautions against equating geometry robustness with control robustness: the released PLDM aggregate has \code{clean\_effective\_rank} $= 130.13$, \code{geometry\_flag = robust}, \transres{}\_l2 $= 0.4710$, and \idprobe{} $= 0.7570$, yet control success under px+goal 0.08 remains $10.00\%$. The released source-of-truth is \dataset{canonical\_external\_baselines\_20260520.json}.

\end{document}
