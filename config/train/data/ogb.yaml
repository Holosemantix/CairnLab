dataset:
  name: ogbench/cube_single_expert
  num_steps: ${eval:'max(${wm.num_preds}, ${loss.rollout.steps}) + ${wm.history_size}'}
  frameskip: 5
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
  keys_to_merge:
    proprio: proprio
