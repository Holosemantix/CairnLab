dataset:
  num_steps: ${eval:'max(${wm.num_preds}, ${loss.rollout.horizon}) + ${wm.history_size}'}
  frameskip: 5
  name: tworoom
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio
