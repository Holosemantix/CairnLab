\documentclass[11pt]{article}

%% --- Geometry / typography ---------------------------------------------------
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

%% --- Math / typography helpers ----------------------------------------------
\usepackage{amsmath,amssymb,amsthm}
\usepackage{bm}

%% --- Tables, figures, captions ----------------------------------------------
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{array}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}

%% --- Code listing -----------------------------------------------------------
\usepackage{xcolor}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  columns=fullflexible,
  keywordstyle=\color{blue!60!black}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{purple!50!black},
  language=Python,
  showstringspaces=false,
  numberstyle=\tiny\color{gray},
  frame=single, framerule=0.3pt, framesep=4pt,
  backgroundcolor=\color{gray!4},
  xleftmargin=4pt,
  literate={σ}{{$\sigma$}}1 {ρ}{{$\rho$}}1 {ε}{{$\varepsilon$}}1
}

%% --- Hyperlinks / refs ------------------------------------------------------
\usepackage[hidelinks]{hyperref}
\usepackage{url}
\usepackage{cleveref}

%% --- Misc -------------------------------------------------------------------
\usepackage{enumitem}
\setlist{nosep,leftmargin=*}
\usepackage[normalem]{ulem}

%% --- Macros -----------------------------------------------------------------
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\stdmax}{\code{std\_max}}
\newcommand{\fragility}{\code{predictor\_target\_to\_nn\_cos\_ratio\_at\_max\_std}}
\newcommand{\transres}{\code{transition\_resolution\_ratio}}
\newcommand{\idprobe}{\code{id\_probe\_r{}\textsuperscript{2}}}

\title{\textbf{Understanding the Invariance--Resolution Trade-off in Latent\\Predictive World Models:\\A Diagnostic Study of JEPA-Based Control}}
\author{}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Joint-Embedding Predictive Architectures (JEPAs) are commonly \emph{believed} to learn abstract, invariant world representations: by predicting in latent space rather than reconstructing pixels, the encoder is expected to discard visual redundancy and noise. This expectation is a \emph{community heuristic} rather than a published guarantee --- to our knowledge no JEPA work formally claims pixel-noise robustness for control. We test the implicit assumption on \textbf{LeWorldModel (LeWM)}~\cite{maes2026lewm}, a published JEPA world model, across four manipulation and navigation tasks (PushT, TwoRoom, Reacher, Cube) and eight levels of train-time pixel-noise augmentation. We find three things:
\begin{enumerate}
  \item \textbf{Visual OOD collapse in JEPA + CEM control.} Without noise-aware training, LeWM collapses under mild pixel noise: PushT control success drops from $86.33\%$ (clean) to $4.67\%$ (Gaussian \mbox{std = 0.08}, near-random); TwoRoom drops from $94.00\%$ to $50.00\%$.
  \item \textbf{No global-optimal noise level exists.} Tasks respond very differently to noise augmentation: visually redundant TwoRoom prefers heavy noise (best at \stdmax{} $= 0.008$), while contact-heavy PushT peaks on \emph{clean} at \stdmax{} $= 0.003$ but on \emph{robustness} at \stdmax{} $= 0.006$ --- clean and robust optima dissociate within a single task.
  \item \textbf{A five-layer diagnostic protocol explains the compression mechanism.} Instrumenting encoder shift, encoder geometry, predictor sensitivity, latent-noise response, and task resolution traces noise-induced control failure to a representational chain: representation compression (drop in effective rank) $\rightarrow$ loss of transition-key resolution (\transres{}) $\rightarrow$ loss of controllability (\idprobe{}). However, used as a \emph{cross-checkpoint} predictor, the strongest single diagnostic (\fragility{}) tracks \emph{overall ckpt quality on clean inputs}, not OOD-specific robustness. The diagnostic toolkit usefully ranks checkpoints by clean control fidelity but does not substitute for actually training with noise when OOD robustness is the goal.
\end{enumerate}
This paper does not propose a new training algorithm. Its contribution is (i) a systematic empirical study of JEPA + CEM visual OOD failure, (ii) a reproducible diagnostic toolkit, and (iii) an honest delineation of what cross-ckpt diagnostics can and cannot predict.

\smallskip
\noindent\textbf{Keywords:} world models; JEPA; visual robustness; representation diagnostics; invariance--resolution trade-off.
\end{abstract}

%% ===========================================================================
\section{Introduction}\label{sec:intro}

\subsection{The implicit invariance heuristic and where it breaks}\label{sec:intro-heuristic}

Since Yann LeCun proposed JEPA~\cite{lecun2022path}, the paradigm has been advanced as a direction for self-supervised learning. Unlike generative models (VAEs, diffusion), JEPA does not reconstruct pixels; it predicts future \emph{representations} in latent space. The motivating intuition --- that predicting ``what is invariant'' rather than ``what pixels look like'' yields abstract representations that discard visual redundancy and noise --- is now part of the field's informal vocabulary in talks, blog posts, and survey articles~\cite{assran2023ijepa,bardes2024vjepa}.

We emphasise that this is a heuristic rather than a published guarantee. To our knowledge no JEPA paper has \emph{formally claimed} visual-OOD robustness for control-relevant downstream tasks. I-JEPA~\cite{assran2023ijepa} and V-JEPA~\cite{bardes2024vjepa,assran2025vjepa2} established strong visual representations on ImageNet and video via masked prediction; LeWorldModel (LeWM)~\cite{maes2026lewm} extended the framework to end-to-end stable world-model training across four robotic control tasks. Existing robustness studies probe JEPA only on image classification~\cite{njepa2025}, synthetic 1D distractors~\cite{huang2026vjepa}, or medical ultrasound~\cite{usjepa2025}; none on \emph{JEPA-based control} under realistic pixel noise.

This leaves a basic operational question open: \textbf{if the input image is degraded by sensor noise, lighting change, or camera jitter, does a JEPA + CEM world model still plan and act reliably?}

The data say no. On PushT (2D pushing), the untrained-with-noise LeWM achieves $86.33\%$ on clean images but falls to $4.67\%$ under Gaussian pixel noise of \mbox{std = 0.08} --- essentially random. TwoRoom (2D navigation) drops from $94.00\%$ to $50.00\%$. Latent prediction alone does not, in this regime, confer the visual robustness the community heuristic would predict.

\subsection{The core tension: no globally optimal noise level}\label{sec:intro-tension}

A natural remedy is input-side noise augmentation during training, a technique long-validated in supervised and contrastive learning~\cite{chen2020simclr,cubuk2020randaugment}. But a deeper question arises: \textbf{does there exist a single, universal noise level that is optimal across tasks?}

A systematic sweep across four tasks at eight levels of \stdmax{} $\in \{0.001, \dots, 0.008\}$ answers no:
\begin{itemize}
  \item \textbf{TwoRoom} (visually redundant navigation): clean success rises monotonically with noise, peaking at \mbox{\stdmax{} = 0.008} ($98.33\%$ / $98.67\%$).
  \item \textbf{PushT} (contact-heavy manipulation): clean peaks at \mbox{\stdmax{} = 0.003} ($89.67\%$), but robustness at px+goal 0.08 peaks at \mbox{\stdmax{} = 0.006} ($87.00\%$) --- clean and robust optima dissociate.
  \item \textbf{Reacher} (continuous reaching): best at \mbox{\stdmax{} = 0.006} ($86.00\%$ / $84.67\%$); very low noise ($0.001$) is statistically indistinguishable from base.
  \item \textbf{Cube} (structured manipulation): noise sweep is weakest; no monotone trend on clean.
\end{itemize}

This exposes a fundamental tension: \textbf{global noise augmentation cannot distinguish ``background visual redundancy that should be made invariant'' from ``control-relevant features that should retain resolution''.}

\subsection{Contributions}\label{sec:intro-contributions}

\paragraph{C1.} A systematic quantification of visual-OOD fragility of JEPA + CEM control across four representative tasks. We sweep 4 tasks $\times$ 8 noise levels with a unified sample budget of 300 trajectories per condition (either 1 seed $\times$ 300 episodes or 3 seeds $\times$ 100 episodes).

\paragraph{C2.} The ``invariance--resolution trade-off'' concept and a five-layer diagnostic protocol that operationalises it. We define five complementary diagnostic layers (encoder shift, encoder geometry, predictor sensitivity, latent-noise response, task resolution) with 17+ concrete metrics, validated by Spearman correlations on the canonical $n = 8$ LeWM checkpoint set and a 9-level within-method sweep, with partial correlations conditioned on training noise to separate ckpt-quality signal from confounding-by-protocol.

\paragraph{C3.} A mechanistic account of why global noise augmentation has limited returns. The diagnostic layers show that on TwoRoom the gains come from desirable representation compression (drop in effective rank) --- a low-dimensional discrete task does not need high resolution --- whereas in PushT the same compression at heavy noise drives \transres{} from $0.30$ to $0.10$ and \idprobe{} from $0.77$ to $0.27$, erasing task-relevant state information.

\paragraph{C4.} An honest delineation of cross-ckpt diagnostics. The strongest cross-checkpoint diagnostic (\fragility{}) predicts \emph{overall ckpt quality} (Spearman $\rho \approx -0.73$ on the $n = 9$ LeWM PushT sweep against clean success rate; the same metric correlates with px+goal 0.08 success at partial $\rho \approx -0.67$ after conditioning on training noise). However, the apparent $\rho(\text{metric}, \text{OOD drop}) = -0.77$ is mediated by \stdmax{}: after partialling out \stdmax{}, that correlation drops to $+0.13$. The metric is a model-selection tool within a fixed training protocol, not an OOD oracle (\Cref{sec:diag-cross,sec:disc-boundary}).

\subsection{Organisation}
\Cref{sec:related} reviews related work; \Cref{sec:bg} defines the LeWM background, noise protocol, and diagnostic framework; \Cref{sec:exp} reports the experimental findings; \Cref{sec:disc} discusses mechanism and implications; \Cref{sec:conc} concludes.

%% ===========================================================================
\section{Related Work}\label{sec:related}

\subsection{JEPA and latent world models}
JEPA~\cite{lecun2022path} predicts in latent space rather than reconstructing pixels. I-JEPA~\cite{assran2023ijepa} predicts target representations from a masked context; V-JEPA~\cite{bardes2024vjepa,assran2025vjepa2} extends the framework to video understanding and video-driven world modelling; LeWM~\cite{maes2026lewm} is the first end-to-end stable JEPA world model, using \textbf{SIGReg} (Sketch Isotropic Gaussian Regularizer) --- random projections plus Epps--Pulley empirical-characteristic-function matching~\cite{eppspulley1983} --- to prevent representation collapse without batch normalisation. LeWM is our baseline. The original LeWM paper reports a Violation-of-Expectation experiment showing the model is sensitive to physical perturbations (object teleportation) but not to visual perturbations (colour change). VoE however measures prediction error (surprise), not control success rate, and colour change differs from pixel-level Gaussian noise. We give the first picture, to our knowledge, of LeWM's control success rate under pixel-noise corruption.

\subsection{Robustness studies of JEPA}
N-JEPA~\cite{njepa2025} introduces diffusion-noise augmentation into I-JEPA, improving linear-probing robustness on ImageNet. VJEPA~\cite{huang2026vjepa} tests a Noisy-TV distractor on synthetic 1D signals and reports JEPA retains $R^2 > 0.84$ under high noise. US-JEPA~\cite{usjepa2025} tests Gaussian blur, contrast reduction, and speckle noise on medical ultrasound. None of these studies the pixel-noise robustness of a JEPA \emph{world model} on \emph{robotic control}. Furthermore, VJEPA's optimistic conclusion ($R^2 > 0.84$ in 1D) contrasts with our control-time observation (success rate $\to 4.67\%$), suggesting that the ``natural robustness'' of JEPA may be an artefact of evaluation modality.

\subsection{World models and input augmentation}
In RL world-model literature, DreamerV3~\cite{hafner2024dreamerv3} and TD-MPC2~\cite{hansen2024tdmpc2} rely on convolutional inductive bias for some implicit noise tolerance. ViGMO~\cite{vigmo} tests Gaussian noise and blur on DMC tasks, finds ``sensor noise is a fundamentally different distribution shift'', and proposes a latent-consistency loss. ViGMO addresses model-based RL, not JEPA architectures; its conclusion is directionally aligned with ours, but our contribution adds mechanistic decomposition via the five-layer diagnostic, not just performance reporting.

\subsection{The invariance--resolution tension}
Tamkin et al.~\cite{tamkin2022featuredropout} argue, in the contrastive-learning context, that label-destroying augmentations can be useful --- augmentations act as feature dropout rather than pure invariance inducers. Zhang et al.~\cite{zhang2022rethinkaug} note that overly strong augmentation imposes excess invariance and erases fine-grained downstream information. These insights are well-established in contrastive learning for classification, but their manifestation in \emph{latent predictive world models} --- where the downstream task is planning rather than discrimination --- has not been systematically studied. The present paper extends the discussion from classification to control with a quantitative framework.

\subsection{Representation diagnostics and collapse analysis}
Self-supervised learning broadly uses effective rank~\cite{roy2007effrank}, condition number, and participation ratio to diagnose dimensional collapse~\cite{jing2022dimcollapse}. Next-Latent Prediction~\cite{teoh2025nextlatent} uses effective latent rank to assess world-model compactness. VICReg~\cite{bardes2022vicreg} provides an anti-collapse regularizer distinct from SIGReg. Individual metrics are not new; our contribution is to combine them into a coherent five-layer protocol with per-token noise sensitivity, cross-ckpt correlation, and a partial-correlation validation scheme conditioning on training noise.

%% ===========================================================================
\section{Background and Diagnostic Framework}\label{sec:bg}

\subsection{LeWorldModel baseline}\label{sec:bg-lewm}

LeWM~\cite{maes2026lewm} is an end-to-end JEPA world model trained with two terms only:
\begin{equation}
  \mathcal{L}_{\text{LeWM}} \;=\; \mathcal{L}_{\text{pred}} \;+\; \lambda \,\cdot\, \mathcal{L}_{\text{SIGReg}}\,,
  \label{eq:lewm-loss}
\end{equation}
where $\mathcal{L}_{\text{pred}}$ is the latent-space MSE between predicted and target representations. SIGReg projects each latent onto $M$ unit-norm random directions, computes the Epps--Pulley empirical-characteristic-function distance~\cite{eppspulley1983} between each projection and $\mathcal{N}(0, 1)$, and aggregates with Gauss-window weights, preventing collapse without explicit BatchNorm. (The Cramér--Wold theorem motivates the construction: equality of high-dimensional distributions reduces to equality of all one-dimensional projections of their characteristic functions.) Inference uses the Cross-Entropy Method (CEM) for latent-space MPC.

\subsection{Input-side noise augmentation}\label{sec:bg-noise}

We add per-frame Gaussian noise to the LeWM input pipeline via \code{utils.py::AddNormalizedGaussianNoise} (\Cref{sec:appendix-A}). Each frame is independently noised: $\mathrm{Bernoulli}(p)$ decides whether the frame is corrupted, and if so the standard deviation is drawn from $\mathrm{Uniform}(0, \text{std\_max})$. We fix $p = 1.0$ and sweep \stdmax{} $\in \{0.001, \dots, 0.008\}$ (eight levels). Evaluation uses two noised conditions: pixels+goal $0.05$ and pixels+goal $0.08$ (Gaussian noise of the indicated std applied to both pixels and the goal image).

\subsection{Five-layer diagnostic framework}\label{sec:bg-five-layers}

\paragraph{Layer 1 --- Encoder shift.} Quantifies the magnitude and direction of latent displacement under input noise. Metrics: \code{noise\_angle\_deg}, \code{noise\_l2}, \code{noise\_to\_nn\_cos\_ratio}, \code{noise\_angle\_slope}.

\paragraph{Layer 2 --- Encoder geometry.} Quantifies global latent-space structure. Metrics: \code{clean\_nn\_cos\_dist}, \code{clean\_effective\_rank}, \code{cka\_linear\_at\_max\_std}.

\paragraph{Layer 3 --- Predictor sensitivity.} Quantifies how the predictor amplifies input noise. Metrics: \fragility{} (single-step predictor target shift normalised by clean NN distance --- the strongest cross-ckpt diagnostic we identify), \code{predictor\_rollout\_drift\_T(T)}.

\paragraph{Layer 4 --- Latent-noise response.} Injects noise directly in the latent $z$ (bypassing the encoder) to isolate predictor and cost contributions. Metrics: \code{latent\_cost\_surface\_slope\_z}, \code{latent\_robust\_radius\_z}.

\paragraph{Layer 5 --- Task resolution.} Quantifies control-relevant information retained by the latent. Metrics: \transres{}\_cos, \transres{}\_l2, \idprobe{} (a controllability proxy).

\subsection{Cross-checkpoint validation protocol}\label{sec:bg-cross-ckpt}

To rule out spurious correlations we adopt two complementary analyses on the same LeWM PushT sweep:

\begin{description}
  \item[Canonical $n = 8$.] Eight LeWM checkpoints (1 base + 7 representative noise levels). Compute Pearson and Spearman correlations of each diagnostic with clean and OOD success rates.
  \item[$n = 9$ LeWM sweep with partial correlation.] All 9 LeWM PushT checkpoints (base + \stdmax{} $\in \{0.001, \dots, 0.008\}$, with the 3-seed retrained 0to006 used in the canonical set). Compute Spearman $\rho$ and \emph{partial} Spearman conditioned on \stdmax{}. The partialling step matters: many diagnostics correlate with control performance only because both quantities co-vary with the training noise level along the sweep.
\end{description}

We report both the raw Spearman and the partial-on-\stdmax{} quantities. The raw $\rho$ tells you ``which checkpoint over the entire sweep behaves better''; the partial $\rho$ tells you ``which checkpoint \emph{within a fixed training protocol} behaves better''. The two questions are distinct, and we will see in \Cref{sec:diag-cross} that they have markedly different answers for the same metric. A larger cross-architecture protocol (varying the latent geometry of the world model itself) is left to future work; we will add a non-JEPA baseline (DreamerV3 or TD-MPC2 on the same tasks) in a follow-up version.

%% ===========================================================================
\section{Experiments}\label{sec:exp}

\subsection{Setup}\label{sec:exp-setup}

\paragraph{Tasks.} PushT (2D pushing), TwoRoom (2D navigation), Reacher (2D arm), Cube (3D manipulation).

\paragraph{Baselines.} LeWM-base (no noise) and LeWM+noise (8-level sweep).

\paragraph{Training.} Each configuration trained with 3 random seeds (42 / 43 / 44); each seed evaluated on 100 trajectories.

\paragraph{Clean-metric definition.} All ``clean'' and noised success rates report a unified \textbf{3-seed $\times$ 100} protocol (seeds 42/43/44, \code{num\_eval=100} each, $300$ trajectories total per condition); every cell of Tables~\ref{tab:base},~\ref{tab:sweep-clean},~\ref{tab:sweep-px08} is mean $\pm$ across-seed std over $n = 3$. The earlier mixed convention (single-seed $\times$ 300 for some checkpoints, 3-seed $\times$ 100 for others) has been retired: all 36 checkpoints (4 tasks $\times$ \{base, \stdmax{} $0.001$--$0.008$\}) have been re-evaluated under this unified protocol. Per-seed raw metrics live at \code{<ckpt>/eval\_results/<cond>\_seed\{42,43,44\}\_metrics.txt}; aggregates are dumped to \code{canonical\_evals\_20260517.json}.

\paragraph{Hardware.} Single NVIDIA A100 (80\,GB); training takes 2--4\,h per task per configuration.

\paragraph{Figures.} Six main figures are produced by \code{tools/paper1\_figs.py}. \Cref{fig:hero,fig:sweep,fig:scatter,fig:radar,fig:mechanism,fig:pareto} are referenced in the relevant sections.

\subsection{JEPA control fragility under visual OOD}\label{sec:exp-cliff}

\Cref{tab:ood-cliff} reports LeWM-base success rates under clean and noised evaluation (mean $\pm$ std across 3 seeds $\times$ 100 evaluations).

\begin{table}[H]
\centering
\caption{LeWM-base under visual OOD (mean $\pm$ std; 3 seeds $\times$ 100 evaluations).}
\label{tab:ood-cliff}
\small
\begin{tabular}{lrrrr}
\toprule
Task & clean & px+goal 0.05 & px+goal 0.08 & clean $\to$ 0.08 drop \\
\midrule
TwoRoom & $94.00 \pm 3.56$ & $61.33 \pm 5.31$ & $50.00 \pm 1.41$ & $\mathbf{-44.00}$ \\
PushT   & $86.33 \pm 2.36$ & $12.00 \pm 4.55$ & $\phantom{0}4.67 \pm 2.05$ & $\mathbf{-81.66}$ \\
Reacher & $58.67 \pm 1.25$ & $27.00 \pm 5.10$ & $15.00 \pm 2.16$ & $\mathbf{-43.67}$ \\
Cube    & $66.67 \pm 2.62$ & $53.33 \pm 3.30$ & $46.33 \pm 3.68$ & $\mathbf{-20.34}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{fig1_hero.png}
\caption{Visual OOD cliff in LeWM and recovery by noise training (success rate, 4 tasks). Blue: LeWM-base clean. Red: LeWM-base under px+g 0.08 noise. Green: best LeWM+noise configuration under px+g 0.08. Annotations show base drop and recovery in points; $\sigma^\ast$ marks each task's optimal \stdmax{}.}
\label{fig:hero}
\end{figure}

LeWM-base is strong on clean images (especially TwoRoom and PushT), but visual std $= 0.05$ applied to pixels and goal jointly already produces large drops on all tasks: PushT loses $74+$ pts (down to near-random $4.67\%$ at std $= 0.08$), TwoRoom $30+$ pts, Reacher $30+$ pts, Cube $\sim 13$ pts (at std $= 0.05$). \textbf{This is not a marginal phenomenon}: a JEPA + CEM world model without noise-aware training has essentially no resistance to visual corruption. The drop pattern across tasks is informative: Cube degrades least ($-20.34$\,pt at std $= 0.08$) --- structured manipulation has some natural robustness --- while PushT degrades most catastrophically ($-81.66$\,pt), confirming that contact-heavy continuous control is most sensitive to visual precision.

\subsection{Noise augmentation closes the gap --- at the cost of task-specific tuning}\label{sec:exp-sweep}

\Cref{tab:sweep-clean,tab:sweep-px08} report the complete 8-level sweep across tasks. All cells are success rate $\pm$ across-seed std (in pts), aggregated under the unified $n = 3$ seeds (42/43/44) $\times$ 100 trajectories protocol --- $300$ trajectories per cell.

\begin{table}[H]
\centering
\caption{LeWM+noise sweep, \textbf{clean} success rate (\%).}
\label{tab:sweep-clean}
\small
\begin{tabular}{lrrrr}
\toprule
\stdmax{} & TwoRoom & PushT & Reacher & Cube \\
\midrule
0 (base) & $94.00 \pm 3.56$ & $86.33 \pm 2.36$ & $58.67 \pm 1.25$ & $66.67 \pm 2.62$ \\
0.001 & $93.67 \pm 3.30$ & $88.00 \pm 3.74$ & $61.67 \pm 2.49$ & $69.33 \pm 0.47$ \\
0.002 & $95.00 \pm 2.83$ & $88.33 \pm 2.62$ & $85.67 \pm 2.49$ & $60.00 \pm 1.63$ \\
0.003 & $96.33 \pm 3.30$ & $\mathbf{89.67 \pm 1.70}$ & $78.67 \pm 1.25$ & $65.00 \pm 1.63$ \\
0.004 & $96.33 \pm 2.05$ & $89.33 \pm 2.05$ & $84.00 \pm 2.94$ & $\mathbf{69.00 \pm 3.74}$ \\
0.005 & $96.00 \pm 2.83$ & $80.67 \pm 4.78$ & $70.00 \pm 2.16$ & $59.33 \pm 0.94$ \\
\textbf{0.006} & $96.67 \pm 2.05$ & $89.33 \pm 2.05$ & $\mathbf{86.00 \pm 2.94}$ & $66.67 \pm 2.05$ \\
0.007 & $96.00 \pm 1.63$ & $85.67 \pm 3.09$ & $83.67 \pm 3.30$ & $67.67 \pm 0.94$ \\
\textbf{0.008} & $\mathbf{98.33 \pm 0.47}$ & $88.33 \pm 2.87$ & $84.00 \pm 0.82$ & $62.33 \pm 1.25$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{LeWM+noise sweep, \textbf{px+goal 0.08} success rate (\%).}
\label{tab:sweep-px08}
\small
\begin{tabular}{lrrrr}
\toprule
\stdmax{} & TwoRoom & PushT & Reacher & Cube \\
\midrule
0 (base) & $50.00 \pm 1.41$ & $\phantom{0}4.67 \pm 2.05$ & $15.00 \pm 2.16$ & $46.33 \pm 3.68$ \\
0.001 & $87.67 \pm 1.89$ & $43.33 \pm 3.09$ & $46.00 \pm 1.63$ & $51.33 \pm 5.79$ \\
0.002 & $93.33 \pm 0.94$ & $71.33 \pm 3.68$ & $85.67 \pm 1.70$ & $60.67 \pm 0.47$ \\
0.003 & $94.67 \pm 2.87$ & $83.00 \pm 3.74$ & $73.67 \pm 0.47$ & $\mathbf{67.33 \pm 1.89}$ \\
0.004 & $95.00 \pm 2.45$ & $81.33 \pm 2.87$ & $80.00 \pm 1.41$ & $67.00 \pm 3.56$ \\
0.005 & $95.67 \pm 2.36$ & $75.00 \pm 6.48$ & $68.00 \pm 3.56$ & $59.67 \pm 2.05$ \\
\textbf{0.006} & $96.67 \pm 2.49$ & $\mathbf{87.00 \pm 3.74}$ & $\mathbf{84.67 \pm 4.03}$ & $65.00 \pm 2.94$ \\
0.007 & $96.33 \pm 2.05$ & $82.33 \pm 4.64$ & $81.33 \pm 1.25$ & $68.00 \pm 1.41$ \\
\textbf{0.008} & $\mathbf{98.67 \pm 0.94}$ & $85.33 \pm 2.62$ & $83.00 \pm 4.32$ & $60.33 \pm 0.94$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Reading guidance.} Differences of $\le 3$\,pts within a column are within $\sim 1$--$2$ std and should not be interpreted as meaningful; the per-task optima (bolded) sit $\ge 5$\,pts above their nearest neighbour in all four task columns.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig2_sweep.png}
\caption{Noise-training sweep: clean vs OOD per task. Dashed vertical lines mark each task's optimum $\sigma^\ast$. No single \stdmax{} is jointly optimal across tasks.}
\label{fig:sweep}
\end{figure}

Three observations follow.

\paragraph{(1) No single \stdmax{} is jointly optimal; clean and robust optima can dissociate within a task.}
TwoRoom peaks globally at \stdmax{} $= 0.008$ ($98.33$ / $98.67$); clean rises monotonically with noise. PushT peaks on clean at \stdmax{} $= 0.003$ but on robustness at \stdmax{} $= 0.006$ ($87.00$ vs.\ $0.002$'s $71.33$; $+15.67$ pt). Reacher peaks at \stdmax{} $= 0.006$ ($86.00$ / $84.67$); at \stdmax{} $= 0.001$ clean is $61.67$ vs base $58.67$ --- within across-seed std ($\sim 2.5$ pts), so the data support \emph{``low noise is statistically equivalent to base''}, not ``low noise hurts''. The inflection occurs at \stdmax{} $= 0.002$ (jump to $85.67$), suggesting Reacher needs a \emph{minimum} invariance threshold. Cube responds least: clean is non-monotonic, and px+g 0.08 improves only in the $0.003$--$0.007$ range.

\paragraph{(2) Per-task tuning is necessary, not optional.}
Optimal \stdmax{} varies substantially: TwoRoom $0.008$ (heavy), PushT clean $0.003$ / robust $0.006$, Reacher $0.006$, Cube robust $0.003$ / clean $\sim 0.001$ or $0.004$. Global input-side noise is the strongest ``global'' form of invariance pressure, but closing the OOD gap requires per-task tuning cost.

\paragraph{(3) Tasks form a clear sensitivity gradient.}
PushT ($-81.66$ base drop) $>$ Reacher ($-43.67$) $>$ TwoRoom ($-44.00$) $>$ Cube ($-20.34$). Recovery does not scale with sensitivity: TwoRoom recovers most fully ($+48.67$ pt at $\stdmax = 0.008$), Cube least ($+21.00$ pt). Input-side global noise is most effective on visually redundant tasks and gives diminishing returns on structured manipulation.

Plotting the same sweep as a (clean, OOD) trajectory per task (\Cref{fig:pareto}) makes the trade-off visually explicit: each task's sweep curve moves from base far below the $y = x$ diagonal toward the upper-right, with per-task curvature determined by how much OOD gain a task can buy from a given drop in clean.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{fig6_pareto.png}
\caption{Per-task Pareto trajectory of (clean, OOD) under noise sweep. Open marker $=$ base; solid markers $=$ \stdmax{} sweep (colour-by-\stdmax{}); ringed marker $=$ per-task best. Dashed line $y = x$.}
\label{fig:pareto}
\end{figure}

\subsection{Diagnostic analysis: why global noise is not a silver bullet}\label{sec:exp-diag}

\Cref{tab:diag-base-vs-best} compares core diagnostic metrics on LeWM-base versus LeWM+noise (per-task best).

\begin{table}[H]
\centering
\caption{Representation diagnostics: LeWM-base vs.\ a representative noise-trained ckpt per task. The $\sigma$ pick per task (0.008 / 0.002 / 0.006 / 0.001) is the ckpt on which the diagnostic suite was originally executed. Under the unified 3-seed $\times$ 100 eval protocol the PushT \emph{clean}-best shifts to \stdmax{} $= 0.003$ (within $\pm 2$\,pt of \stdmax{} $= 0.002$); we keep the diagnostics on the \stdmax{} $= 0.002$ ckpt because the compression-vs.-resolution pattern this table illustrates is robust within this neighbourhood.}
\label{tab:diag-base-vs-best}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrrrrr}
\toprule
Metric & TwoRoom & TwoRoom & PushT & PushT & Reacher & Reacher & Cube & Cube \\
       & base & noise (0.008) & base & noise (0.002) & base & noise (0.006) & base & noise (0.001) \\
\midrule
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.0281 & 0.2360 & 0.1051 & 0.0633 & 0.0676 & 0.1856 & 0.1879 \\
\code{clean\_effective\_rank}       & 47.60  & 33.59  & 76.42  & 42.85  & 61.04  & 65.92  & 73.25  & 71.83  \\
\transres{}\_l2  & 0.7216 & 0.6055 & 0.3015 & 0.2800 & 0.3704 & 0.3791 & 0.4847 & 0.4629 \\
\transres{}\_cos & 0.5538 & 0.3780 & 0.0868 & 0.0800 & 0.1351 & 0.1399 & 0.2347 & 0.2168 \\
\code{id\_probe\_r2}            & 0.2889 & $-0.0573$ & 0.7739 & 0.7500 & 0.1621 & 0.1729 & 0.6657 & 0.6720 \\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.4482 & 0.1283 & 0.1200 & 0.2518 & 0.2585 & 0.2364 & 0.2320 \\
\code{predictor\_rollout\_T8\_l2}      & 18.62  & 17.90  & 18.65  & 16.50  & 15.17  & 0.44   & 20.20  & 19.25  \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{Notes.} (i) \transres{}\_l2 and \_cos values for TwoRoom are corrected to the values directly stored in \code{geometry\_summary.json} / \code{task\_resolution.json}; (ii) Reacher and Cube best diagnostics are taken from the corresponding ckpt's \code{eval\_results/diagnostics/\{geometry,task,predictor\}*.json} (max-std $= 0.1$, history-only noise); (iii) Cube base \code{predictor\_rollout\_T8\_l2} $= 20.20$ and Reacher base $= 15.17$ are comparable, but the recovery effect of noise training on long-horizon rollout drift is highly task-dependent: Reacher best $= 0.44$ (35$\times$ reduction); Cube best $= 19.25$ (almost unchanged).

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{fig4_radar.png}
\caption{Per-task diagnostic radar: base vs noise-best on 6 metrics, min-max normalised across all 4 tasks.}
\label{fig:radar}
\end{figure}

\paragraph{Mechanistic reading.}
\begin{itemize}
  \item \textbf{TwoRoom.} Low-dimensional, discrete, visually redundant. Compressing the representation (effective rank $47.6 \to 33.6$) is acceptable and even beneficial: a more compact latent space is easier to plan in.
  \item \textbf{PushT.} Continuous contact requires fine-grained pose resolution. Even at the light noise of the diagnostic ckpt (\stdmax{} $= 0.002$), \transres{}\_l2 already trends slightly downward. At heavier noise (e.g.\ $0.006$) the metric would drop further, erasing contact-transition keyframes.
  \item \textbf{Predictor-rollout caveat.} A drop in \code{predictor\_rollout\_T8\_l2} is not unambiguously good news. It can also mean the latent has become more \emph{predictable} without being more \emph{controllable}: predictor stability bought by sacrificing resolution.
\end{itemize}

\subsection{Cross-checkpoint correlation analysis}\label{sec:diag-cross}

\Cref{tab:corr-n8} reports task-specific cross-ckpt correlations for the strongest candidate metrics on the canonical $n = 8$ set.

\begin{table}[H]
\centering
\caption{Diagnostic metric $\leftrightarrow$ eval correlation (canonical $n = 8$).}
\label{tab:corr-n8}
\small
\begin{tabular}{lrrrr}
\toprule
Metric & TwoRoom ($r$/$\rho$) & PushT ($r$/$\rho$) & Reacher ($r$/$\rho$) & Cube ($r$/$\rho$) \\
\midrule
\fragility{}                 & $-0.96$/$-0.43$ & $\mathbf{-0.80}$/$\mathbf{-0.93}$ & $-0.56$/$-0.58$ & $+0.17$/$+0.04$ \\
\code{latent\_cost\_surface\_slope\_z}                  & $+0.47$/$+0.61$ & $\mathbf{+0.74}$/$\mathbf{+0.93}$ & $-0.20$/$-0.14$ & $-0.28$/$-0.37$ \\
\code{predictor\_rollout\_T8\_l2}                       & $+0.22$/$+0.23$ & $+0.68$/$+0.79$ & $\mathbf{-0.71}$/$\mathbf{-0.83}$ & $+0.41$/$+0.76$ \\
\code{cka\_linear\_at\_max\_std}                        & $+0.58$/$+0.29$ & $-0.08$/$-0.02$ & $+0.92$/$+0.68$ & $\mathbf{-0.85}$/$\mathbf{-0.96}$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{$n = 9$ LeWM PushT sweep, with partial correlation conditioned on \stdmax{}.}

To strengthen the analysis we replace eval-only correlation with partial correlation conditioned on the training noise level. On the $n = 9$ LeWM PushT sweep (canonical ckpts including the 3-seed retrained 0to006), the strongest single per-token diagnostic, \fragility{}, gives the following picture (\Cref{tab:partial-corr}).

\begin{table}[H]
\centering
\caption{Spearman $\rho$ (unconditional) and partial Spearman $\rho$ conditioned on \stdmax{}, for the fragility metric on the $n = 9$ LeWM PushT sweep.}
\label{tab:partial-corr}
\small
\begin{tabular}{lr}
\toprule
Quantity & Spearman $\rho$ \\
\midrule
$\rho(\stdmax{}, \text{metric})$ & $+0.83$ \\
$\rho(\stdmax{}, \text{clean})$ & $-0.43$ \\
$\rho(\stdmax{}, \text{px+goal } 0.08)$ & $+0.82$ \\
$\rho(\stdmax{}, \text{OOD drop})$ & $-0.93$ \\
\midrule
$\rho(\text{metric}, \text{clean})$ unconditional & $\mathbf{-0.73}$ \\
$\rho(\text{metric}, \text{clean}) \mid \stdmax{}$ partial & $\mathbf{-0.73}$ \\
$\rho(\text{metric}, \text{px+g } 0.08)$ unconditional & $+0.55$ \\
$\rho(\text{metric}, \text{px+g } 0.08) \mid \stdmax{}$ partial & $\mathbf{-0.67}$ \\
$\rho(\text{metric}, \text{OOD drop})$ unconditional & $-0.77$ \\
$\rho(\text{metric}, \text{OOD drop}) \mid \stdmax{}$ partial & $+0.13$ \\
\bottomrule
\end{tabular}
\end{table}

The honest interpretation:
\begin{enumerate}
  \item \textbf{Within a fixed training protocol the metric strongly predicts both clean and OOD performance.} Partial $\rho$ on clean is $-0.73$ and on px+goal 0.08 is $-0.67$ --- both meaningful negative correlations. Among models trained at the \emph{same} \stdmax{}, the one with lower fragility ratio achieves better clean \emph{and} better noisy success.
  \item \textbf{The metric does NOT predict OOD drop beyond training protocol.} The unconditional $\rho(\text{metric}, \text{drop}) = -0.77$ looks impressive, but partialling out \stdmax{} collapses it to $+0.13$. The drop's strong correlation with the metric is a mediated effect: \stdmax{} drives both the metric ($\rho = +0.83$) and the drop ($\rho = -0.93$). Once \stdmax{} is fixed, the metric tells you nothing new about how much the \emph{gap} between clean and noisy performance will be.
  \item \textbf{Practical reading.} The toolkit is a model-selection tool that ranks checkpoints within a fixed training protocol. It is \emph{not} a substitute for actually training with noise when the OOD gap is the quantity of interest.
\end{enumerate}

\subsubsection{What this diagnostic actually predicts: clean vs OOD}\label{sec:diag-clean-vs-ood}

Older versions of this analysis reported only the unconditional $\rho$ between the fragility metric and ``eval''. Earlier internal write-ups described that quantity as predicting OOD failure; the partial-correlation analysis above shows the more nuanced reality:

\begin{itemize}
  \item The metric is a \textbf{strong ckpt-quality signal} --- within any fixed training protocol, lower fragility ratio means better control on \emph{both} clean and noisy evaluation (partial $\rho \approx -0.7$ on both).
  \item The metric \textbf{does not isolate noise-robustness} --- its apparent correlation with the gap between clean and noisy success is fully mediated by \stdmax{}.
\end{itemize}

The PushT scatter (\Cref{fig:scatter}) makes both halves of this visible. Panel (a) plots metric vs clean, showing a strong negative trend (Spearman $\rho = -0.73$). Panel (b) plots metric vs OOD drop, showing the same trend at unconditional $\rho = -0.77$, but the colour-bar (encoding \stdmax{}) reveals the structure: low-\stdmax{} ckpts (light blue) live at high drop, high-\stdmax{} ckpts (dark blue) live at low drop, and the metric tracks \stdmax{} itself. Once \stdmax{} is held constant, the metric does not separate small-drop from large-drop ckpts.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig3_scatter.png}
\caption{PushT $n = 9$ LeWM sweep: fragility metric is a ckpt-quality predictor (a), the apparent OOD-drop correlation in (b) is mediated by \stdmax{} (encoded by the colour bar).}
\label{fig:scatter}
\end{figure}

\subsection{Mechanism attribution: where in the pipeline does noise cause failure?}\label{sec:exp-mech}

\Cref{sec:exp-diag} reports \emph{what} is compressed and \Cref{sec:diag-cross} reports \emph{which diagnostics} correlate with eval across checkpoints, but neither answers \textbf{``is the failure in the encoder, the predictor, or the cost surface?''} We address this with two complementary experiments.

\subsubsection{Eval-only cost swap: cost surface is not the main culprit}\label{sec:mech-cost-swap}

If the failure were primarily caused by the planning-time cost function (for instance, cosine cost saturating under noise), changing the cost should produce a substantial recovery. We hold a TwoRoom checkpoint fixed and only swap the CEM cost type at evaluation time. Variant A is the canonical inference configuration; variant B is an alternative-cost ablation (\Cref{tab:cost-swap}).

\begin{table}[H]
\centering
\caption{Eval-only cost swap on a TwoRoom checkpoint, std $= 0.03$ pixels+goal.}
\label{tab:cost-swap}
\small
\begin{tabular}{llll r}
\toprule
Variant & Cost type & Cost space & Success rate \\
\midrule
A (default) & cosine & normalized & 36.0 \\
B (swap)    & mse    & raw        & 42.0 \\
Reference: same checkpoint, clean eval ($\text{num\_eval} = 300$) & --- & --- & 69.7 \\
\bottomrule
\end{tabular}
\end{table}

Swapping cost recovers only $+6$\,pt ($36 \to 42$), far below the clean baseline ($69.7$). \textbf{Conclusion: cost surface is not the main cause.} Upstream noisy-goal embedding corruption sets the ceiling.

\subsubsection{Latent-noise probing: encoder is the principal bottleneck}\label{sec:mech-latent-probing}

Directly injecting noise into the latent $z$ (skipping the encoder) decouples encoder contributions from predictor + cost. Comparing diagnostic signals across two injection points (pixels vs.\ latent $z$, both history-only noise at max std):

\begin{itemize}
  \item \textbf{TwoRoom.} \code{latent\_predictor\_rollout\_T8\_l2\_history} $\leftrightarrow$ eval $\rho = +0.738$, slightly stronger than input-space \code{predictor\_rollout\_T8\_l2} ($\rho = +0.667$) --- encoder remains dominant but predictor adds an independent contribution.
  \item \textbf{PushT.} Input-space and latent-only signals are nearly collinear ($+0.627$ / $+0.636$); the single-step \fragility{} ($\rho = -0.791$) is the strongest. Encoder + single-step predictor jointly dominate; cost surface (\code{latent\_cost\_surface\_slope\_z}, $\rho = +0.93$) is collinear with the latent rollout.
  \item \textbf{Reacher / Cube.} \code{latent\_cost\_surface\_slope\_z} $|\rho| < 0.4$ --- cost surface is not an explanatory variable.
\end{itemize}

\Cref{tab:attribution} summarises the three-layer attribution.

\begin{table}[H]
\centering
\caption{Three-layer attribution by task.}
\label{tab:attribution}
\small
\begin{tabular}{lll}
\toprule
Task & Primary cause & Secondary \\
\midrule
TwoRoom & encoder dominant & independent predictor contribution \\
PushT   & encoder + single-step predictor & cost surface (collinear with latent rollout) \\
Reacher & encoder + multi-step rollout & --- \\
Cube    & encoder & --- \\
\bottomrule
\end{tabular}
\end{table}

The common primary cause across the four tasks is \textbf{encoder shift transduced by the predictor}; \textbf{cost surface is not a principal explanatory variable on any task}. This is also the root reason Layer-5 task-resolution metrics carry strong signal in \Cref{sec:exp-diag}: once the encoder's latent-neighbourhood structure is corrupted beyond the NN distance scale, downstream predictor and planner are already operating on the wrong neighbourhood.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{fig5_mechanism.png}
\caption{Mechanism attribution. Left: three-layer attribution flow (pixels $\to$ encoder $\to$ predictor $\to$ cost / CEM). Right: $|\rho|$ of the strongest per-layer signal vs eval drop per task ($n = 8$).}
\label{fig:mechanism}
\end{figure}

%% ===========================================================================
\section{Discussion}\label{sec:disc}

\subsection{Rethinking the ``JEPA invariance'' narrative}\label{sec:disc-narrative}

The data challenge an implicit community assumption: latent prediction alone is not sufficient to confer invariance to visual noise. The invariance a JEPA encoder acquires is \emph{in-distribution invariance} --- invariance to visual patterns present in the training distribution. When test-time inputs carry high-frequency pixel noise not seen during training, the topology of the latent space breaks down, the predictor outputs incorrect future states, and the planner fails.

This stands in contrast to VJEPA~\cite{huang2026vjepa}, which reports $R^2 > 0.84$ under a Noisy-TV distractor --- but that result is on 1D synthetic signals evaluated by linear probe. \textbf{Control-task evaluation (success rate) is a strictly stricter standard than linear-probe $R^2$}: linear probes only need enough information for a linear classifier to extract, whereas control tasks demand a representation that supports precise latent-space planning and action optimisation.

\subsection{The invariance--resolution trade-off --- manifestation and scope}\label{sec:disc-scope}

The trade-off has the following four-task signature in this paper:

\begin{itemize}
  \item \textbf{TwoRoom.} Background wall colour / texture is pure visual redundancy; discarding it does not impair navigation.
  \item \textbf{PushT.} The pixel changes during T-block / arm contact carry critical force and pose information; discarding them blinds the planner.
  \item \textbf{Reacher.} Low-dimensional continuous control; visual encoding of joint angle requires moderate resolution.
  \item \textbf{Cube.} Object pose and grasp-point spatial relations need moderate resolution, but Cube itself is largely insensitive to global noise (action sequence is structured; visual--action coupling is predictable).
\end{itemize}

Task-specificity explains why there is no single optimal noise level.

\paragraph{Scope.} Whether the trade-off generalises to other latent world-model architectures is an open question we do not address. Reconstruction-based world models (DreamerV3~\cite{hafner2024dreamerv3} / TD-MPC2~\cite{hansen2024tdmpc2}) explicitly force preservation of pixel information; ViGMO~\cite{vigmo} observes related task-specific noise sensitivity on DMC. EMA-target JEPA (I-JEPA / V-JEPA lineage) and variational JEPA may exhibit different dynamics. This paper's scope is LeWM + CEM; universalising the trade-off exceeds the present evidence.

\subsection{When the diagnostic toolkit works --- and when it does not}\label{sec:disc-boundary}

Three boundaries should be stated up-front so practitioners know when to use the toolkit and when to fall back to direct evaluation.

\paragraph{Boundary 1: the toolkit ranks ckpts by clean-control quality, not by OOD-specific robustness.}
\Cref{sec:diag-clean-vs-ood} shows the strongest cross-ckpt diagnostic (\fragility{}) correlates strongly with clean success ($\rho \approx -0.8$) but only weakly with OOD drop ($\rho \approx -0.3$). Use the toolkit to pick the \emph{best-trained} checkpoint from a sweep; do not use it as a substitute for an actual OOD eval if your downstream question is robustness.

\paragraph{Boundary 2: tasks with weak per-state controllability variance fall outside the toolkit's strict regime.}
Reacher and TwoRoom do not produce diagnostic-vs-eval Spearman $\rho$ that survives the partial-correlation criterion. Both tasks lack the within-protocol spread to distinguish ``good'' from ``bad'' ckpts via a label-free metric. The toolkit can describe what these models have compressed (\Cref{tab:diag-base-vs-best}) but cannot predict relative checkpoint quality.

\paragraph{Boundary 3: cross-ckpt diagnostics cannot recover what training did not provide.}
The largest determinant of OOD drop is whether the model saw noise during training --- not which fragility metric it happens to score on. This is the structural reason $\rho(\text{metric}, \text{drop})$ is weak even when $\rho(\text{metric}, \text{clean})$ is strong: noise vs no-noise training puts each ckpt on a completely different (clean, OOD) curve (cf.\ \Cref{fig:pareto}), and no static cross-ckpt diagnostic can substitute for that training-time choice.

The toolkit therefore has a precise scope: it is a \emph{clean-evaluation auxiliary} that lets you select among ckpts on tasks with strong per-state controllability variance (PushT, Cube), assuming the training protocol is already fixed. It is not an OOD prediction oracle.

\subsection{Practical guidance: how to choose \stdmax{} on a new task}\label{sec:disc-practical}

Our sweep suggests a simple operational recipe:
\begin{enumerate}
  \item Inspect the clean baseline's \fragility{}. If $< 10^{-5}$ (PushT / Cube regime), the task is pixel-noise sensitive; start sweeping at \stdmax{} $\in [0.001, 0.003]$ with clean as primary constraint.
  \item Check \code{clean\_effective\_rank} and \transres{}. High rank, high ratio (PushT: 76 / 0.30) $\to$ resource-rich; cap the sweep at $0.005$. Low rank, low ratio (TwoRoom: 47 / 0.72) $\to$ visually redundant; sweep safely up to $0.008+$.
  \item Use \emph{two endpoints} in evaluation (clean and max-noise). Checking only one misses one of the optima: PushT's clean optimum at $0.003$ vs robustness optimum at $0.006$ is the clearest example.
  \item Under compute budget, a 4-level sweep (\{0.001, 0.003, 0.005, 0.007\}) already locates the optimum within $\pm 0.001$.
\end{enumerate}

\subsection{Limitations and future directions}\label{sec:disc-limit}

\paragraph{Limitation 1 --- Single backbone family.} We validate on LeWM. Other JEPA variants (EMA-target I-JEPA / V-JEPA lineage; variational JEPA) may exhibit different noise responses.

\paragraph{Limitation 2 --- Gaussian pixel noise only.} Real-world visual corruption includes motion blur, contrast variation, occlusion, and lighting change; whether the trade-off transfers is open.

\paragraph{Limitation 3 --- Diagnostic framework is empirical, not theoretical.} Reacher and TwoRoom fail the partial-correlation criterion for \emph{all} metrics, exposing where the empirical framework breaks down.

\paragraph{Limitation 4 --- Automated transition reweighting is not a substitute for noise training.} As a sanity check we also tested a scale-preserving heteroscedastic-NLL formulation in which a learned per-transition $\sigma$-head down-weights high-error transitions. The result is informative as a negative finding: TwoRoom clean reaches $99.67\%$ but high-noise robustness lags noise training; PushT clean \emph{collapses} from $86.33\%$ to $13.33\%$ because contact-control transitions have high prediction error and are exactly the transitions $\sigma$-weighting would discard. Full data are reported in \Cref{sec:appendix-D}. The lesson --- ``hard $\ne$ unimportant'' in contact control --- connects this paper's trade-off to the broader question of per-token controllers, which we leave to follow-up work.

\paragraph{Future direction 1 --- Per-token adaptive consistency.} \Cref{sec:diag-cross,sec:exp-mech} identify the strongest signal \fragility{}, a ckpt-level scalar. Whether its per-token variant can serve as a per-token consistency controller is an open methodological question, under investigation in follow-up work.

\paragraph{Future direction 2 --- Cross-architecture replication.} Running the sweep and diagnostic protocol on DreamerV3 / TD-MPC2 would reveal whether the trade-off is JEPA-specific or shared by all latent-compression world models.

\paragraph{Future direction 3 --- Theoretical grounding.} Reformulating the trade-off in an information-bottleneck / rate-distortion language is an attractive direction; we did not pursue it because the empirical phenomenology may not yet be stable enough for formal modelling.

%% ===========================================================================
\section{Conclusion}\label{sec:conc}

This paper provides a systematic diagnostic study of the visual-OOD control robustness of JEPA + CEM world models, using LeWorldModel as the representative system. Three findings:

\begin{enumerate}
  \item Visual OOD collapse in JEPA + CEM control. Without noise-aware training, LeWM collapses under pixel noise; latent prediction alone does not confer visual robustness.
  \item Global noise augmentation has a boundary. It effectively closes the OOD gap but has no globally optimal level; task differences are large, and within a single task clean and robustness optima can dissociate.
  \item A five-layer diagnostic protocol reveals the underlying mechanism. Noise-augmentation gains arise from representation compression, but excessive compression destroys the resolution required for control --- this is the \emph{invariance--resolution trade-off}. When used cross-ckpt, the strongest single diagnostic (\fragility{}) predicts clean control quality rather than OOD-specific robustness; we delineate this scope honestly.
\end{enumerate}

This paper does not propose a new training algorithm. Its contribution is a systematic body of empirical evidence, a reusable diagnostic toolkit, and an explicit delineation of what cross-ckpt diagnostics can and cannot predict.

%% ===========================================================================
\section*{Acknowledgements}
The complete code and data are available at \url{https://github.com/qun-team/wm_exp}. We thank the LeWM authors for releasing the baseline framework on which this study builds.

%% ===========================================================================
\bibliographystyle{plain}
\bibliography{references}

%% ===========================================================================
\appendix
\section{Experimental details}\label{sec:appendix-A}

\subsection{Environments}
\begin{itemize}
  \item \textbf{PushT.} 2D continuous pushing; 20{,}000 expert episodes; $\sim$\,196 steps/episode; action dim 2 (direction + magnitude); 224$\times$224 RGB.
  \item \textbf{TwoRoom.} 2D continuous navigation; 10{,}000 episodes; $\sim$\,92 steps; action dim 2; 224$\times$224 RGB.
  \item \textbf{Reacher.} 2D arm control (DeepMind Control Suite); 10{,}000 episodes; 200 steps; action dim 2.
  \item \textbf{Cube.} 3D cube manipulation (OGBench); 10{,}000 episodes; 200 steps; action dim 7.
\end{itemize}

\subsection{Noise augmentation implementation}
The actual implementation is \code{utils.py::AddNormalizedGaussianNoise}. Key points: (i) noise is added on ImageNet-normalized tensors and must be divided by the per-channel std to retain ``pixel-space std'' semantics; (ii) sampling is per-frame independent over the leading frame dims, not per-batch; (iii) all sweeps in this paper fix \code{noise\_prob} $= 1.0$ and \code{std\_min} $= 0$ and vary only \stdmax{}.

\begin{lstlisting}
class AddNormalizedGaussianNoise:
    """Per-frame independent. Each frame draws Bernoulli(noise_prob) then
    std ~ Uniform(std_min, std_max). Pixel-space std is converted to
    normalized space by dividing by per-channel std before adding."""
    def __init__(self, std_min, std_max, noise_prob=1.0):
        self.std_low, self.std_high, self.noise_prob = std_min, std_max, noise_prob
        self.channel_std = torch.as_tensor(IMAGENET_STD)  # (C,)

    def __call__(self, x):
        if self.std_high <= 0 or self.noise_prob <= 0:
            return x
        leading = x.shape[:-3]
        stds = torch.empty(leading, device=x.device, dtype=x.dtype).uniform_(
            self.std_low, self.std_high)
        if self.noise_prob < 1.0:
            mask = (torch.rand(leading, device=x.device) < self.noise_prob).to(x.dtype)
            stds = stds * mask
        per_frame_scale = stds.view(*leading, 1, 1, 1)
        channel_factor = (1.0 / self.channel_std.to(x.device, x.dtype)).view(
            *([1] * len(leading)), -1, 1, 1)
        scale = per_frame_scale * channel_factor   # pixel-space std -> normalized
        return x + torch.randn_like(x) * scale
\end{lstlisting}

\subsection{Evaluation protocol}
Clean evaluation uses no noise, 100 trajectories per seed $\times$ 3 seeds (42/43/44), totalling 300 trajectories per condition. Noisy evaluation adds Gaussian noise of std $\in \{0.05, 0.08\}$ to both pixels and the goal image under the same 3-seed protocol. Success criterion is task-specific (PushT: T-block pose match; TwoRoom: target region; Reacher: joint-angle match; Cube: cube position match).

\subsection{Compute}
All experiments run on a single NVIDIA A100 (80\,GB). Training: $\sim$2\,h/task for LeWM-base, $\sim$2.5\,h/task/configuration for LeWM+noise. Diagnostic analysis: $\sim$30\,min/task/checkpoint.

\subsection{Main-figure rendering}
The six main figures are produced by \code{tools/paper1\_figs.py}. Run \code{python -m tools.paper1\_figs --out-dir assets/paper1\_figs}. The script reads \code{summary.txt} and \code{eval\_results/diagnostics/*.json} from each checkpoint; no model loading is required.

%% ===========================================================================
\section{Full diagnostic profile of LeWM-base on four tasks}\label{sec:appendix-B}

\Cref{tab:full-diag} summarises every core diagnostic on the four LeWM-base checkpoints. Data are taken from each checkpoint's \code{eval\_results/diagnostics/\{geometry,task,predictor,latent\_noise\}*.json}.

\begin{table}[H]
\centering
\caption{Full LeWM-base diagnostics across four tasks.}
\label{tab:full-diag}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrr}
\toprule
Layer / Metric & TwoRoom & PushT & Reacher & Cube & Unit / note \\
\midrule
\multicolumn{6}{l}{\textit{Encoder geometry}}\\
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.2360 & 0.0633 & 0.1856 & cosine distance \\
\code{clean\_pair\_cos\_dist\_median} & 0.9904 & 1.0228 & 1.0252 & 1.0193 & pair-wise cos dist \\
\code{clean\_effective\_rank} & 47.60 & 76.42 & 61.04 & 73.25 & effective rank \\
\midrule
\multicolumn{6}{l}{\textit{Noise sensitivity}}\\
\code{noise\_angle\_deg\_median} (@\,std$=0.005$) & $5.51^{\circ}$ & $1.33^{\circ}$ & $3.22^{\circ}$ & $1.40^{\circ}$ & median angular shift \\
\code{noise\_to\_nn\_cos\_ratio\_median} & 0.1031 & 0.0011 & 0.0249 & 0.0016 & noise/NN cos ratio \\
\code{robust\_radius\_std} & 0.0142 & 0.0537 & 0.0142 & 0.0356 & critical noise std \\
\code{first\_risk\_std} & $>$0.08 & $>$0.08 & $>$0.08 & $>$0.08 & first high-risk std \\
\code{noise\_angle\_slope\_deg\_per\_std} & 1085.8 & 284.8 & 831.7 & 327.0 & $^{\circ}$/std angular gain \\
\code{geometry\_flag} & balanced & robust & balanced & robust & geometry label \\
\midrule
\multicolumn{6}{l}{\textit{Task resolution}}\\
\transres{}\_l2  & 0.7216 & 0.3015 & 0.3704 & 0.4847 & L2 resolution ratio \\
\transres{}\_cos & 0.5538 & 0.0868 & 0.1351 & 0.2347 & cos resolution ratio \\
\code{id\_probe\_r2} & 0.2889 & 0.7739 & 0.1621 & 0.6657 & linear probe $R^{2}$ \\
\code{id\_probe\_r2\_min} & 0.2599 & 0.6786 & 0.1366 & 0.0972 & min probe $R^{2}$ \\
\code{lidar\_rank} & 46.06 & 13.95 & 45.90 & 42.46 & LiDAR rank proxy \\
\midrule
\multicolumn{6}{l}{\textit{Action effect}}\\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.1283 & 0.2518 & 0.2364 & action-perturb mean shift \\
\code{action\_perturb\_pred\_shift\_corr} & 0.2847 & 0.2873 & 0.4042 & 0.2559 & shift $\times$ $\|a\|$ corr \\
\midrule
\multicolumn{6}{l}{\textit{Predictor stability}}\\
\code{predictor\_rollout\_T8\_l2} & 18.62 & 18.65 & 15.17 & 20.20 & T=8 rollout L2 drift \\
\fragility{} & 1.51e-4 & 3.54e-6 & 2.67e-5 & 3.39e-6 & max std target/NN ratio \\
\midrule
\multicolumn{6}{l}{\textit{Latent noise}}\\
\code{cka\_linear\_at\_max\_std} & 0.1986 & 0.5536 & 0.3085 & 0.1814 & CKA clean vs noisy \\
\code{latent\_cost\_surface\_slope\_z} & 635.31 & 1.3886 & 599.45 & 0.6208 & goal latent cost slope \\
\bottomrule
\end{tabular}
\end{table}

%% ===========================================================================
\section{Heteroscedastic-loss formulation}\label{sec:appendix-C}

The scale-preserving heteroscedastic NLL referenced in \Cref{sec:disc-limit} (Limitation 4) and detailed in \Cref{sec:appendix-D} is:
\begin{equation}
  \mathcal{L}_{\text{hetero}} = \tfrac{1}{2}\,\exp(-s_t)\,\| z_{t+1} - \hat z_{t+1}\|^2 + \tfrac{1}{2}\, s_t \,,
  \label{eq:hetero}
\end{equation}
where $s_t$ is the $\sigma$-head-predicted log-variance. During training $\exp(-s_t)$ acts as an automatic weight: high-error transitions are down-weighted and low-error transitions are up-weighted. When $s_t \equiv 0$ the loss reduces to plain MSE.

%% ===========================================================================
\section{Heteroscedastic-loss negative result (data)}\label{sec:appendix-D}

This appendix records the full data behind \Cref{sec:disc-limit} Limitation 4. The $\sigma$-head is trained jointly with the predictor; gradient is detached on the $\sigma$ path so the mean prediction path is exactly LeWM MSE when $\sigma$ is constant.

\begin{table}[H]
\centering
\caption{Heteroscedastic-loss evaluation.}
\label{tab:hetero-eval}
\small
\begin{tabular}{lrrrrrr}
\toprule
Task / model & Clean & goal 0.05 & pixels 0.05 & px+goal 0.05 & goal 0.08 & px+goal 0.08 \\
\midrule
TwoRoom LeWM-base       & 94.00 & 73.33 & 72.33 & 61.33 & 58.67 & 50.00 \\
TwoRoom LeWM+noise best & 98.33 & 98.00 & 98.33 & 98.00 & 98.67 & 98.67 \\
TwoRoom hetero          & \textbf{99.67} & 85.33 & 96.67 & 84.67 & 73.33 & 55.33 \\
PushT LeWM-base         & 86.33 & 38.00 & 17.00 & 12.00 & 11.33 &  4.67 \\
PushT LeWM+noise best   & \textbf{89.33} & 87.67 & 88.00 & 88.33 & 89.67 & 87.00 \\
\textbf{PushT hetero}   & \textbf{13.33} &  7.67 &  7.67 &  7.67 &  9.67 &  6.00 \\
\bottomrule
\end{tabular}
\end{table}

TwoRoom hetero reaches $99.67\%$ clean --- consistent with the prior that low-dimensional discrete tasks benefit from stronger invariance / clustering --- but lags noise training on high-noise robustness. \textbf{PushT hetero clean is $13.33\%$ --- a method-level failure}, not a robustness trade-off.

\begin{table}[H]
\centering
\caption{Representation diagnostics of the hetero-loss failure.}
\label{tab:hetero-diag}
\small
\begin{tabular}{lrrrr}
\toprule
Metric & TwoRoom base & TwoRoom hetero & PushT base & PushT hetero \\
\midrule
\code{clean\_nn\_cos\_dist\_median} & 0.0449 & 0.0281 & 0.2360 & 0.1051 \\
\code{clean\_effective\_rank}       & 47.60  & 33.59  & 76.42  & 42.85  \\
\transres{}\_l2 & 0.5538 & 0.3780 & 0.3015 & \textbf{0.1023} \\
\code{id\_probe\_r2}            & 0.2889 & $-0.0573$ & 0.7739 & \textbf{0.2678} \\
\code{action\_mean\_pred\_shift\_norm} & 0.5329 & 0.4482 & 0.1283 & 0.0841 \\
\code{predictor\_rollout\_T8\_l2}      & 18.62  & 17.90  & 18.65  & 14.01  \\
\bottomrule
\end{tabular}
\end{table}

Hetero-loss compresses the representation in both tasks. For TwoRoom (low-dimensional, discrete, redundant), compression is acceptable. For PushT, \transres{}\_l2 collapses from $0.3015$ to $0.1023$ and \idprobe{} from $0.7739$ to $0.2678$ --- task-relevant state information is erased. The drop in \code{predictor\_rollout\_T8\_l2} is not good news either: the latent has become more \emph{predictable} without being more \emph{controllable}.

\paragraph{Mechanism.} The $\sigma$-head correctly learns per-transition difficulty (high $\sigma$ on contact frames in PushT, on doorway crossings in TwoRoom, etc.), but using $\sigma$ as an automatic weight to down-weight high-error transitions misclassifies the contact-and-control-critical states of PushT as ``unimportant'' and erases them. This is a direct demonstration of the broader trade-off lesson: in contact-heavy control, \textbf{hard $\ne$ unimportant}.

This finding motivates a follow-up direction we are currently exploring: per-token \emph{consistency} (not loss-reweighting) controllers driven by detached difficulty signals, which preserve the mean prediction path's gradient distribution. That work is independent of this paper's diagnostic study and will be reported separately.

\end{document}
